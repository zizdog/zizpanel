# 从镜像站一键离线安装 / 打包迁移（离线包格式 + 打包工具 + 离线模式）

> 目标：回答"应用市场里的软件，能不能在**整机断外网**的情况下从镜像站装起来"，
> 以及"还差哪些文件才能让当时的 27 个全部离线可装"（应用目录现已 42 条，数字随目录增长）。
>
> 本文所有数字与日志都是**真跑出来的**；没跑过的一律写「未验证」。
> 配套的只读清点见 `docs/应用市场下载点清点.md`（另一个代理产出，讲"平时从哪下、多快"）；
> 本文讲**离线**这一层：格式、打包工具、面板开关、缺口。
>
> 时间：2026-09-16 17:00–17:40。代码基线 `467ad37` + 工作区未提交改动（有并发代理）。
> 本文快照当时**没有** commit、**没有**部署、**没有**跑 `make check`；其中 §3.1 的离线模式改动
> **已随后续面板升级上线**（现状见 §3.1 / §3.2 / §5 的注）。

---

## 1. 离线包格式（镜像站侧）

### 1.1 布局

```
<镜像基址>/offline/<app-id>/index.json                ← 有哪些版本、current 是哪个
<镜像基址>/offline/<app-id>/<version>/manifest.json    ← 本应用安装所需的**全部**文件
<镜像基址>/offline/<app-id>/<version>/artifacts/...     ← 真实文件（sha256 实算）
```

- `<version>` 优先用**真实版本号**（brew 类是 `stable + revision`，如 `9.0.1_1`；release 二进制是 tag，如 `v0.71.0`）；
  实在没有版本概念的（如 iopaint 的模型）用 `current`。
- `/offline/` 是镜像站上的**静态目录**（`<镜像根>/offline/`，容器把整个
  镜像根 bind mount 成 `html` 根）→ **不需要改 nginx.conf、不需要重启任何容器**，
  `<镜像基址>/offline/...` 直接可读。
- 因此离线包与 `apps/`、`models/`、`brew/` 是**并列的静态路径**，也不会和别的代理正在改的
  `nginx.conf`（pypi 那些 location）抢同一份文件。

### 1.2 manifest.json 字段

| 字段 | 含义 |
|---|---|
| `schema` | `zizpanel.offline/v1`（对不上的版本要明确拒绝，不许硬解） |
| `app` / `name` / `version` | 应用 ID、显示名、**真实版本** |
| `install_method` | `brew` / `binary_release` / `compose` / `site` / `panel_installer` / `colima` |
| `brew` | 仅 brew 类：`formula` / `api_dir` / `bottle_dir`（下面 §1.4 讲怎么用） |
| `docker_images` | 仅 compose 类：compose 文件里引用的镜像（原样，未加 registry 前缀） |
| `artifacts[]` | **本应用安装所需的每一个文件**，见下 |
| `missing[]` | 构建时**取不到**的文件（逐条，含原因）—— 不许静默跳过 |
| `warnings[]` | 取到了但有问题的（如镜像缺 OCI manifest，brew 会退化成 full relocation） |
| `gaps[]` | **设计上还没镜像**的东西（如 compose 镜像 tar、PyPI 依赖闭包） |
| `totals` | `{artifacts, bytes, missing}`，与 artifacts/missing 逐个对齐 |

`artifacts[]` 里每个文件一定有：

| 字段 | 含义 |
|---|---|
| `kind` | `brew_bottle` / `brew_manifest` / `brew_formula_json` / `pip_wheel` / `model` / `site_tarball` / `github_binary` / `docker_image_tar` / `vm_image` / `other` |
| `path` | 在离线包里的**相对路径**（= 离线安装时它必须被 serve 的路径），如 `artifacts/brew/ffmpeg-9.0.1_1.arm64_sequoia.bottle.tar.gz` |
| `url` | **上游地址，仅作参考**（离线安装永远不用它；留着是为了缺件时告诉人去哪补） |
| `source_url` | 这次实际从哪取的（正常是镜像站自己） |
| `sha256` | ★ **下载落盘后真算出来的**，不是抄的（见 §2.3） |
| `size` | 实测字节数 |
| 其它 | `formula` / `tag` / `pkg_version` / `bottle_rebuild` / `note`（brew 类） |

### 1.3 真实样例（字段格式）

> 下面是真实清单样例，**地址已换成占位符 `<镜像基址>`**（面板不内置任何固定镜像地址）。

`<镜像基址>/offline/frpc/v0.71.0/manifest.json`：

```json
{
  "app": "frpc",
  "name": "frpc（frp 客户端）",
  "version": "v0.71.0",
  "install_method": "binary_release",
  "category": "tool",
  "brew": null,
  "docker_images": [],
  "artifacts": [
    {
      "kind": "github_binary",
      "path": "artifacts/frp_0.71.0_darwin_arm64.tar.gz",
      "source_url": "<镜像基址>/apps/frpc/v0.71.0/frp_0.71.0_darwin_arm64.tar.gz",
      "url": "https://github.com/fatedier/frp/releases/download/v0.71.0/frp_0.71.0_darwin_arm64.tar.gz",
      "sha256": "45be02b186860d375ed49a8941ae9569628a54bf14e67fc36b29c98c99dabcc6",
      "size": 12680181,
      "note": "官方 fatedier/frp v0.71.0 darwin-arm64 产物"
    }
  ],
  "missing": [],
  "warnings": [],
  "gaps": [],
  "bytes": 12680181,
  "schema": "zizpanel.offline/v1",
  "generated_at": "2026-09-16T17:15:52+0800",
  "generated_by": "tools/build-offline-bundle.sh",
  "mirror_base": "<镜像基址>",
  "totals": { "artifacts": 1, "bytes": 12680181, "missing": 0 }
}
```

`index.json`（同目录上一级）：

```json
{
  "app": "frpc",
  "name": "frpc（frp 客户端）",
  "current": "v0.71.0",
  "versions": ["v0.71.0"],
  "updated_at": "2026-09-16T17:15:52+0800",
  "mirror_base": "<镜像基址>"
}
```

brew 类的样例（ffmpeg，45 个制品，`kind` 三种：`brew_formula_json` / `brew_bottle` / `brew_manifest`；节选）：

```json
{
  "app": "ffmpeg", "version": "9.0.1_1", "install_method": "brew",
  "brew": { "formula": "ffmpeg", "api_dir": "artifacts/brew/api", "bottle_dir": "artifacts/brew" },
  "artifacts": [
    {
      "kind": "brew_bottle",
      "path": "artifacts/brew/ffmpeg-9.0.1_1.arm64_sequoia.bottle.tar.gz",
      "source_url": "<镜像基址>/brew/ffmpeg-9.0.1_1.arm64_sequoia.bottle.tar.gz",
      "url": "https://ghcr.io/v2/homebrew/core/ffmpeg/blobs/sha256:fe7b941dedd14ad289206c8c53fa16cc18c38e18cc63487f2d2aa98b6217f442",
      "sha256": "fe7b941dedd14ad289206c8c53fa16cc18c38e18cc63487f2d2aa98b6217f442",
      "size": 21727602,
      "formula": "ffmpeg", "tag": "arm64_sequoia", "pkg_version": "9.0.1_1", "bottle_rebuild": 0
    },
    { "kind": "brew_formula_json", "path": "artifacts/brew/api/formula/ffmpeg.json", "size": 5332, "formula": "ffmpeg", "sha256": "<下载后实算>" },
    { "kind": "brew_manifest", "path": "artifacts/brew/ffmpeg/manifests/9.0.1_1", "size": 196012, "formula": "ffmpeg", "sha256": "<下载后实算>" }
  ],
  "totals": { "artifacts": 45, "bytes": 50806191, "missing": 0 }
}
```

### 1.4 brew 类怎么"离线装上"（不是猜的，是读 brew 源码 + 真机日志定的）

把下面两个环境变量指到离线包，`brew install` 就完全不碰外网（**已在断网机器上真机跑通**）：

```
HOMEBREW_API_DOMAIN    = <base>/offline/<app>/<ver>/artifacts/brew/api
HOMEBREW_BOTTLE_DOMAIN = <base>/offline/<app>/<ver>/artifacts/brew
```

对应的文件名规则（逐行核对 Homebrew 7.0.2 的
`bottle.rb` 的 `Bottle::Filename`、`github_packages.rb` 的 `version_rebuild`、
`utils/bottles.rb` 的 `path_resolved_basename`，并用镜像站上 brew 自己的 access log 对照）：

```
pkg_version  = versions.stable + ("_" + revision if revision > 0)
平铺瓶文件名  = "<name>-<pkg_version>.<tag>.bottle[.<bottle_rebuild>].tar.gz"
OCI manifest = "<name 里 @→/ +→x>/manifests/<pkg_version>[-<bottle_rebuild>]"
```

真机对照（镜像站 access log，brew 自己发的请求）：

| 公式 | revision / bottle rebuild | brew 请求的 manifest | brew 请求的瓶 |
|---|---|---|---|
| ffmpeg | 1 / 0 | `/brew/ffmpeg/manifests/9.0.1_1` | `/brew/ffmpeg-9.0.1_1.arm64_sequoia.bottle.tar.gz` |
| abseil | 0 / 1 | `/brew/abseil/manifests/20260817.0-1` | `/brew/abseil-20260817.0.arm64_sequoia.bottle.1.tar.gz` |
| mysql@8.4 | 4 / 0 | `/brew/mysql/8.4/manifests/8.4.11_4` | `/brew/mysql%408.4-8.4.11_4.arm64_sequoia.bottle.tar.gz` |
| php@8.2 | 0 / 0 | `/brew/php/8.2/manifests/8.2.33` | `/brew/php@8.2-8.2.33.arm64_sequoia.bottle.tar.gz` |

> ⚠️ **坑（差点误判成"镜像缺件"）**：只看 `versions.stable` 是不够的，必须带 `revision`。
> 第一版漏了 `revision`，把 ffmpeg 算成 `ffmpeg-9.0.1...`（真名是 `9.0.1_1`），于是报"镜像上没有这个瓶" ——
> 而镜像上其实有。少一个 revision 就会把一个装得上的应用误报成缺件。
>
> ⚠️ **坑（HTTP 侧核对抓出来的）**：`openssl@3` 的瓶，brew 请求的是 `openssl%403-...`（URL 编码），
> nginx 会把 `%40` 解回 `@`，**落盘名是 `openssl@3-...`**。第一版把编码后的名字当成落盘名，
> 磁盘上真出现了一个叫 `%40` 的文件，**本地核对全过、HTTP 侧立刻 404**。
> 这条正好说明"必须从 HTTP 侧重下重算"这条纪律的价值。

---

## 2. 打包工具

### 2.1 组成

| 文件 | 作用 |
|---|---|
| `tools/build-offline-bundle.sh` | 入口：选应用、取件、算 sha256、写清单、上传镜像站、HTTP 侧核对、缺件对账 |
| `tools/offline-bundle-lib.py` | 按计划取件 + 实算 sha256 + 写 manifest/index |
| `tools/offline-brew-resolve.py` | 把一个 brew formula 展开成**依赖闭包**的文件清单（含上面那套命名规则） |
| `internal/services/offline_plan.go` | `OfflinePlan()`：从代码目录/注册表生成**全部在售应用**的**打包计划**（纯函数，不联网） |
| `cmd/zizpanel-assets plan` | 把计划打成 JSON（`apps` 子命令保持原样，兼容 `sync-nas-apps.sh`） |

设计要点（都是踩过的坑）：

- **计划从代码生成，不在脚本里手抄** —— 手抄的那份在加应用/升版本时一定会漏。
- **sha256 一律对落盘文件重算**；上游清单里声明的只用来**判等**，不等就判缺件并失败。
- **缺件逐条列出**，最后按"计划制品数 = 磁盘文件数"对账，并且选中应用数 = 汇总条数。
- **ssh 一律 `-n` + `</dev/null`**：`sync-nas-apps.sh` 曾因为 `ssh` 在 `while read` 里吞掉 stdin
  而静默跳过应用却报成功。
- `--dry-run` 只解析（含真实 HEAD 取体积）不下载，可用来做缺口盘点。

### 2.2 用法

```bash
# 1) 只看计划与已知缺口（不联网下载）
bash tools/build-offline-bundle.sh --list

# 2) 只解析体积（会向镜像发 HEAD，不落盘）
bash tools/build-offline-bundle.sh --dry-run --app ffmpeg

# 3) 本地打包（下载 + 实算 sha256 + 写 manifest；不上传）
bash tools/build-offline-bundle.sh --app frpc --app ffmpeg --stage /tmp/stage

# 4) 打包并上传到镜像站（上传后逐个制品从 HTTP 侧重下重算 sha256）
NAS_PASS='…' bash tools/build-offline-bundle.sh --app frpc --app ffmpeg --upload

# 5) 事后验收（只读镜像站上的 index/manifest，重下重算）
bash tools/build-offline-bundle.sh --verify ffmpeg
```

### 2.3 真实运行输出（本次交付跑的那一轮）

```
==> 读离线打包计划（代码注册表 + 目录，不手抄）：go run ./cmd/zizpanel-assets plan
    计划里共 27 个应用（快照当时的目录）

==> 应用：ffmpeg   version=9.0.1_1  制品=45  缺件=0  体积=50.9MB  ✓ 全部核对通过
==> 应用：ollama   version=0.34.0   制品=36  缺件=0  体积=94.1MB  ✓
==> 应用：frpc     version=v0.71.0  制品=1   缺件=0  体积=12.7MB  ✓
==> 应用：orbien-client v3.6.0      制品=1   缺件=0  体积=2.1MB   ✓
==> 应用：ddns-go  version=v6.17.7  制品=1   缺件=0  体积=4.4MB   ✓
==> 应用：iopaint  version=current  制品=1   缺件=0  体积=205.7MB ✓
==> 汇总：成功 6，失败 0，退出码 0
```

sha256 三个来源**逐字节一致**（实测），这是"真的算过"的证据：

```
镜像 apps/ddns-go/v6.17.7/manifest.json 声明: 9dac9d824dd7e13ceed8c9d54848982a345ae10f9922dbf936631bc293e5d44e
目标机实际下载到的 tarball              : 9dac9d824dd7e13ceed8c9d54848982a345ae10f9922dbf936631bc293e5d44e
离线包 frpc manifest 里的 github_binary : 45be02b186860d375ed49a8941ae9569628a54bf14e67fc36b29c98c99dabcc6
```

---

## 3. 面板侧"离线模式"（做了哪部分 / 没做哪部分）

### 3.1 已做（本次改动；现已随面板升级上线）

| 位置 | 改动 |
|---|---|
| `internal/config/config.go` | 新增设置项 `OfflineOnly bool json:"offline_only"`（附语义注释：与 `mirror_base` 的区别） |
| `internal/services/services.go` | `Options.OfflineOnly` |
| `internal/web/api_services.go` | `svcManager()` 把 `Cfg.OfflineOnly` 注进 Options（每次按当前 Cfg 新建 → 保存后立刻生效） |
| `internal/web/api_system.go` | `settingsView()` 回传 `offline_only`；`settingsReq` 接受它；保存分支写入（并顺手纠正了 `MirrorBase` 那段"唯一来源"的过期注释） |
| `internal/services/mirror.go` | `MirrorOfflineOnly(ctx) bool`、`offlineOnlyFail(resource, mirrorURL) error`、`MirrorOfflinePreflight(ctx, resource, mirrorURL) error` |
| `internal/web/assets/js/views.js` | 「设置 → 访问与安全」新增「仅走镜像站（离线）」勾选框 + "开了离线但没填镜像基址"的即时告警；顺手纠正了镜像卡片里"不会回退 GitHub"的过期文案 |
| `internal/services/offline_plan_test.go` | 4 条新单测（计划覆盖目录里全部应用、每类安装方式都有交代、离线闸门语义）——**全绿** |

`MirrorOfflinePreflight` 的语义（给接线用）：

```go
// 非离线模式 → 直接返回 nil，不改任何既有行为
// 离线模式   → 先 HEAD 镜像上那个具体文件；没有/不通就返回**面向用户**的错误：
//   "离线模式（仅走镜像站）已开启，禁止回落外网，但镜像上没有这个资源：<资源>
//      镜像上应存在的位置：<URL>
//      补法：bash tools/build-offline-bundle.sh --app <应用> --upload … 或关掉这个开关"
```

### 3.2 没做（刻意，避免和并发代理抢文件）

- **当时没有把判断接进任何安装器**。`MirrorOfflineOnly` 只是"一个可被调用的判断"，
  不会自动拦截任何东西 —— 不接线就没有任何行为变化。
  **现状（第九轮后）**：**pip 这条路已接**（`internal/services/pypi_mirror.go` 的 `pipMirrorArgs` 在离线模式下
  探不通就明确失败、不回落清华源；Qwen / IOPaint 都走它）。**whisper 模型这条路也已接**（`internal/services/stt_install.go` 调用 `MirrorOfflinePreflight`）；
  其余回落点（brew / release 二进制 / 站点源码 / IOPaint 权重 / CLT / HF / Docker / colima）仍未接线。
- **现状（已部署后）：已随面板升级上线**，真机上的 `/api/v1/settings` 有 `offline_only` 字段。
  （快照当时确实没有部署，所以那时返回里没有这个字段。）
- **前端开关没有在真实浏览器里点过**。只过了 acorn 语法校验
  （`node tools/check-js-syntax.mjs` → `✓ JS 语法 OK`，当时 27 个文件、现 31 个）和代码审阅；部署上线后是否点过**未复核**。

### 3.3 需要协调的接线清单（请用户/协调方决定由谁改）

每一处都是"**镜像探不通就回落公网**"的决策点，也就是离线模式必须拦住的地方。
行号是本次读取工作区时的位置，可能已被并发代理改动。

| # | 文件 | 函数/位置 | 现在的回落行为 | 离线模式应改成 |
|---|---|---|---|---|
| 1 | `internal/services/install.go` | `probeBrewMirrors` / `brewEnv` | 镜像站 `/brew` → 中科大/清华/阿里云 → 不设域（brew 回落 ghcr.io） | 离线时：镜像站 `/brew` 探不通 → **明确失败**，不试公共镜像、不设空域 |
| 2 | `internal/services/install.go` | `downloadURLsFor` / `preflightMirrorAsset` | 镜像缺件 → 回落 `spec.downloadURLs()`（GitHub + gh-proxy + ghfast） | 离线时：缺件 → 失败并列出镜像应存在的路径 |
| 3 | `internal/services/mirror.go` | `checkMirrorURL` 的调用方（同上两处） | 措辞是"改用公网源" | 离线时走 `MirrorOfflinePreflight` |
| 4 | `internal/services/binary_release.go` | `downloadURLs()`（约 391 行）与下载循环 | 镜像优先，失败回落官方/加速 | 离线时只允许镜像候选 |
| 5 | `internal/services/site_sources.go` | `sitePackageSources`（约 113 行） | 镜像站 `/sites/<app>/<ver>/<file>` 探不到 → 回落官方源 + gh-proxy | 离线时直接失败（typecho/wordpress 目前**完全没镜像**，见 §4） |
| 6 | `internal/services/iopaint.go` | `iopaintWeightSources`（约 407 行） | 镜像站 `/models/iopaint/big-lama.pt` 探不到 → 回落 GitHub release | 离线时失败 |
| 7 | `internal/services/qwentts.go` | `qwenHFEndpoint`（约 502 行） | 镜像站 `/hf` → `hf-mirror.com` → `huggingface.co` | 离线时只允许镜像站 `/hf`（**注意：/hf 是 8091 按需缓存代理，真离线要求该模型已在 `_cache` 里**） |
| 8 | `internal/services/pypi_mirror.go`（Qwen / IOPaint 调用） | `pipMirrorArgs` | 镜像站 `/pypi/simple` 探不通 → 回落清华源 | **已接线（第九轮后）**：离线时返回明确错误，不回落清华源 |
| 9 | `internal/services/homebrew_clt_mirror.go` | `cltMirrorBaseFor`（约 168 行） | 镜像站 `/zizpanel/clt` → `zizdog.com/zizpanel`（公网） | 离线时失败（`zizpanel/clt/072-44426-A/` 当时是空的；**第九轮后载荷已预置 632 MB / 21 片**） |
| 10 | `internal/services/docker_mirror.go` | 镜像加速源列表（docker.1ms.run / daocloud / ustc …） | 多源轮换 | 离线时不应改 registry 配置（改了反而更糟：指向不可达的加速源）；应要求本地 `docker load` tar |
| 11 | `internal/services/colima.go` / `colima_image.go` | `PrewarmColimaGuestImage`、`colimaGuestMirrorURLs` | 期望 `<base>/apps/colima-core/<ver>/manifest.json`（guest 镜像约 317MiB） | 离线时只允许镜像，缺了就失败（该文件目前**没镜像**） |
| 12 | `internal/services/lnmp.go` | 一键 LNMP 的多镜像选择 | 多镜像+公网 | 同 #1 |
| 13 | `internal/services/basedep.go` | 基础依赖（ffmpeg 等）安装 | 走 brew（同 #1） | 同 #1 |
| 14 | `internal/services/voicereceiver.go` | `VoiceSources` / pip | 走 pip 公网 | 同 #8 |

**接线的推荐姿势**（改动最小、不动默认行为）：在每个回落点之前插一行

```go
mirrorURL := m.appAssetURL(spec.ID, spec.Tag, spec.Asset)   // 或该来源的镜像地址
if err := m.MirrorOfflinePreflight(ctx, "frpc 安装包", mirrorURL); err != nil {
    return err
}
```

非离线模式下它是 no-op；离线模式下它会拦住回落并给出可读的错误。

### 3.4 离线部署的镜像基址怎么填

面板默认的 `mirror_base` = `https://mirror.zizdog.com:8888`（公网入口）。所以：

- 平时（有外网）走公网镜像即可；
- **真离线时它不通**（断外网后 `https://mirror.zizdog.com:8888/` → `http=000`）。
  离线/迁移场景要把 `mirror_base` 指向**你自己那台能连通的镜像机**（任意 `http(s)://…`
  地址，例如你内网自建的 `http://<你的镜像机>:8090`），否则"镜像优先"会在第一步探测
  就失败、然后回落到公网 —— 表现为"装不上"而不是"用镜像装上了"。
  面板不内置任何固定镜像地址。

---

## 4. 缺口清单：要让全部应用离线可装，还差什么

> **现状（2026-09-16 第九轮后）**：下表是当时的缺口快照，多数硬缺口已补齐 ——
> `/pypi/simple/`（pip 反代清华）、`/sites/`（Typecho/WordPress）、
> `/zizpanel/clt/`（632 MB / 21 片）、`/models/ollama/`、`/apps/colima-core/v0.10.4/`（guest VM 镜像 + `.sha512sum`）
> 均已上线。保留此表用于理解"离线包格式为什么这么设计"。

图例：✅=已经镜像且已打成离线包（HTTP 实测通过）；🟡=镜像站上有但还没进离线包；❌=还没有，且是硬缺口。

| # | 应用 | 方式 | 离线所需 | 现状 | 体积（实测/估） | 建议镜像路径 |
|---|---|---|---|---|---|---|
| 1 | iopaint | 面板 Python | big-lama.pt + **PyPI 闭包** | 🟡 模型在 `/models/iopaint/big-lama.pt`（已进离线包）；❌ PyPI | 模型 205.7 MB；PyPI 未测 | `models/iopaint/big-lama.pt`（已就绪）；PyPI 走 `/pypi/simple` |
| 2 | qwen3tts | 面板 Python | HF 模型整目录 + **PyPI 闭包** | ❌ 模型只有 8091 按需缓存（`_cache` 2.9 GB），没有静态落盘；❌ PyPI | 模型 2.9 GB；PyPI 未测 | `hf/mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit/...`（静态化） |
| 3 | voicereceiver | 面板 Python | **不需要额外文件**（脚本内嵌在面板二进制里） | ✅ 自包含（唯一外部依赖 ffmpeg 由 ffmpeg 条目覆盖） | 0 | — |
| 4 | phpmyadmin | brew | phpmyadmin 瓶 | ✅ 已解析可打包（1 个公式 3 个制品） | 14.5 MB | `<base>/offline/phpmyadmin/5.2.3/…` |
| 5 | nginx | brew | nginx + 3 依赖瓶 | ✅ 可打包 | 15.2 MB | `<base>/offline/nginx/1.31.6/…` |
| 6 | php83 | brew | php@8.3 **+ 48 个依赖** | ✅ 可打包（依赖闭包已验证齐全） | 249.2 MB | `<base>/offline/php83/8.3.33/…` |
| 7 | php81 | brew | php@8.1 + 48 依赖 | ✅ 可打包 | 247.7 MB | … |
| 8 | php82 | brew | php@8.2 + 48 依赖 | ✅ 可打包 | 248.7 MB | … |
| 9 | php84 | brew | php@8.4 + 48 依赖 | ✅ 可打包 | 135.7 MB | … |
| 10 | mysql84 | brew | mysql@8.4 + 9 依赖 | ✅ 可打包 | 134.1 MB | … |
| 11 | ffmpeg | brew | ffmpeg + 14 依赖瓶 | ✅ **已打包并 HTTP 核对 45 个制品通过** | 50.9 MB | `<base>/offline/ffmpeg/9.0.1_1/…` |
| 12 | ollama | brew | ollama + 11 依赖瓶 | ✅ **已打包并在断网环境真装成功** | 94.1 MB | `<base>/offline/ollama/0.34.0/…` |
| 13 | docker-runtime | brew+VM | colima/lima + **guest VM 镜像 ≈317 MiB** | 🟡 colima brew 闭包可打包（42.2 MB / 2 公式 / 6 制品）；❌ guest 镜像未镜像（面板期望 `apps/colima-core/<ver>/manifest.json`） | brew 42.2 MB；VM ≈317 MiB（**未实测**，引用代码注释） | `apps/colima-core/<ver>/` |
| 14 | uptime-kuma | compose | `louislam/uptime-kuma:1` 镜像 tar | ❌ 未镜像 | **未测** | 自行 `docker save` 成 tar 放进离线包 |
| 15 | minio | compose | `quay.io/minio/minio:latest` | ❌ | **未测** | 同上 |
| 16 | n8n | compose | `n8nio/n8n:latest`（代码里已改为这个；随目录走） | ❌ | **未测** | 同上 |
| 17 | gitea | compose | `gitea/gitea:latest` | ❌ | **未测** | 同上 |
| 18 | stirling-pdf | compose | `stirlingtools/stirling-pdf:latest` | ❌ | **未测** | 同上 |
| 19 | it-tools | compose | `ghcr.io/corentinth/it-tools:latest` | ❌ | **未测** | 同上 |
| 20 | filebrowser | compose | `filebrowser/filebrowser:latest` | ❌ | **未测** | 同上 |
| 21 | frpc | release 二进制 | frp tar.gz | ✅ **已打包 + HTTP 核对通过** | 12.7 MB | `apps/frpc/v0.71.0/`（+ `/offline/frpc/v0.71.0/`） |
| 22 | orbien-client | release 二进制 | orbien tar.gz | ✅ **已打包 + HTTP 核对通过** | 2.1 MB | `apps/orbien-client/v3.6.0/` |
| 23 | ddns-go | release 二进制 | ddns-go tar.gz | ✅ **已打包 + 断网面板真装成功** | 4.2 MB | `apps/ddns-go/v6.17.7/` |
| 24 | typecho | 站点源码 | `typecho.zip` | ❌ 未镜像（镜像路径 `sites/typecho/latest/` 不存在） | **0.58 MB**（经 gh-proxy 实测 content-length=578232） | `sites/typecho/latest/typecho-latest.zip` |
| 25 | wordpress | 站点源码 | `latest-zh_CN.zip` | ❌ 未镜像 | **44.8 MB**（实测 cn.wordpress.org content-length=44762801） | `sites/wordpress/latest/wordpress-latest.zip` |
| 26 | metatube-server | compose | `ghcr.io/metatube-community/metatube-server:latest` | ❌ | **未测** | 自行 `docker save` 成 tar 放进离线包 |
| 27 | squoosh | compose | `pjmeca/squoosh:1.1.0` | ❌ | **未测** | 自行 `docker save` 成 tar 放进离线包 |

### 4.1 汇总

- **已能真离线安装（有硬证据）**：frpc、orbien-client、ddns-go（面板任务中心真装成功）、
  ollama（brew 真装成功）、ffmpeg（45 制品齐全、HTTP 核对通过，本机未重装）。
- **brew 类全部可离线**：9 条 brew 公式（含 phpmyadmin）的依赖闭包**逐条解析过、零缺件**，
  合计约 **1.18 GB**（见下表）。需要时用一条命令就能补进镜像站。
- **硬缺口（要别人补）**：
  1. **9 个 compose 镜像的 tar**（体积未测）→ 镜像站不提供 /docker，需自行 `docker save` 打进离线包；
  2. **PyPI 依赖闭包**（iopaint / qwen3tts）→ `/pypi/` 镜像线；
  3. **Qwen3 TTS 的 HF 模型整目录静态化**（2.9 GB，现在只在 8091 按需缓存里）；
  4. **Colima/Lima guest VM 镜像**（≈317 MiB）→ 自行打包进离线包；
  5. **typecho + wordpress 源码包**（0.58 MB + 44.8 MB）→ 建议 `sites/<app>/latest/`；
  6. **Homebrew CLT 的 pkg 载荷**（`/zizpanel/clt/072-44426-A/` 当时是空目录）——
     这个不在应用目录里，但装 Homebrew 本身要用它。
  > **现状（第九轮后）**：2/3/4/5/6 均已上线（路径见本节开头注）；1 的镜像 tar 需自行 `docker save` 打进离线包，但"9 个 compose 镜像是否全部能离线拉起来"**未在真机断网复核**。
- **离线模式自身的前提**：`mirror_base` 必须指向**你自己那台能连通的镜像机**（公网入口或自建都行）；面板不内置任何固定镜像地址。

brew 闭包明细（真 HEAD 出来的体积）：

| 公式 | 解析版本 | 闭包公式数 | 制品数 | 体积 |
|---|---|---|---|---|
| php@8.3 | 8.3.33 | 49 | 147 | 249.2 MB |
| php@8.2 | 8.2.33 | 49 | 147 | 248.7 MB |
| php@8.1 | 8.1.34 | 49 | 147 | 247.7 MB |
| php@8.4 | 8.4.25 | 49 | 147 | 135.7 MB |
| mysql@8.4 | 8.4.11_4 | 10 | 30 | 134.1 MB |
| ollama | 0.34.0 | 12 | 36 | 94.1 MB |
| ffmpeg | 9.0.1_1 | 15 | 45 | 50.9 MB |
| colima | 0.10.3 | 2 | 6 | 42.2 MB |
| nginx | 1.31.6 | 4 | 12 | 15.2 MB |
| phpmyadmin | 5.2.3 | 1 | 3 | 14.5 MB |
| **合计** | | | | **≈1.18 GB** |

---

## 5. 我没做到 / 没验证的（如实列出）

1. **面板侧的离线模式只接了 pip 与 whisper 模型两条路**（`pypi_mirror.go`、`stt_install.go` 的 `MirrorOfflinePreflight`）；其余 §3.3 的
   回落点打开开关**不会**真的拦住回落。
2. **面板侧改动已随后续面板升级上线**（快照当时没有部署），`offline_only` 设置项在真机上已生效，
   **端到端"打开开关 → 断网 → 缺件明确失败"这条链路仍未在真机上验证过**。
3. **前端勾选框没有在浏览器里点过**（部署后是否点过未复核）。只过了 acorn 语法校验。
4. **ffmpeg 没有在目标机上重装**（它已安装且被 TTS 依赖）；它的离线包只做到
   "45 个制品齐全 + 从 HTTP 侧重下重算 sha256 全通过"，**没有**跑一次真的
   `brew install ffmpeg`。brew 路径本身用 ollama 端到端验证过了。
5. **只对 6 个应用建了离线包**（frpc / orbien-client / ddns-go / iopaint / ffmpeg / ollama）。
   其余 brew 类只做了"解析 + 体积"（零缺件），**没有真的下载落盘**，所以它们的
   体积是 HEAD 出来的真实值，但没有逐一 sha256 核对过。
6. **9 个 compose 镜像、PyPI 闭包、Colima guest 镜像的体积与完整性都没测**（不是我的范围，
   由 `/pypi/` 与自建离线包负责）。表里写"未测"的地方**没有数字**，不是估的。
7. **typecho 的官方源在本次网络下不可达**（`github.com` 直连超时），
   它的 0.58 MB 是走 `gh-proxy.com` 量的；顺带发现目录里给的
   `https://cdn.jsdelivr.net/gh/typecho/typecho@master/typecho.zip` **返回 404**
   （不是我改的范围，但下游会用到，建议核实）。
8. **`/hf/` 是 8091 的按需缓存代理，不是静态文件**。所以"Qwen 模型离线可用"目前
   只能靠"该模型已经在 `_cache` 里"（另一个代理实测已缓存 2.9 GB），
   **没有**做成 `offline/qwen3tts/.../artifacts/` 那样的静态离线包 —— 一旦缓存被清就没了。
9. **没有跑 `make check`**（用户明确要求不跑，有并发代理）。我跑过的是：
   `gofmt -l`（干净）、`go build ./internal/{config,services,web}/ ./cmd/zizpanel-assets`、
   `go vet` 这四个包（干净）、`go test ./internal/services/ -run 'Mirror|Offline'`（全绿）、
   `node tools/check-js-syntax.mjs`（全绿）。**全量测试未跑。**
10. **`offline_only` 与 `mirror_base` 的组合校验只做了"界面提示 + 安装时明确失败"**，
    没有在保存时直接拒绝"离线但没配镜像"（这是刻意的两步操作友好性取舍，见代码注释）。
