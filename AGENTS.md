# ZizPanel — 工作区规则

> DSH 每个新会话自动加载本文件，所以**只放不变的规则**。
> 开工前先读 **`ZizPanel-当前状态.md`**（当前进度/待办/真机结论/凭据，已 gitignore）；
> 要细节再读 **`DEVELOPMENT.md`**（设计取舍、测试策略、发布流程）；坑清单见 **`docs/坑清单.md`**。
> **`README.md` 只给使用者看**，别再往里塞开发过程。
>
> 文档存在的意义是**少占上下文**：每份文件都有行数预算（见第六节）。
> 历史过程不进文档 —— 进 git 历史，或压成 `docs/坑清单.md` 里的一条结论。

## 项目一句话

只做 macOS 的类宝塔面板：Go 单二进制 + 内嵌原生 ESM 前端（**无构建步骤**）+ SQLite，
一条 `curl | sudo bash` 装完即可远程网页操作。

- **本机（MacBook Air M4）＝ 唯一的开发机与测试机**：`https://127.0.0.1:8443/<后缀>`。
- 🛑 **生产机（Mac mini M4）默认不接触**：不部署、不跑测试、不做破坏性验证、不读它的站点/数据库/面板凭据。
  **唯一例外**：公网镜像站 `mirror.zizdog.com:8888` 的文档根在它的外置盘上，而那是**只有它的面板进程才有 TCC 写权限**
  的地方（本机 rsync/sudo 一律 `Operation not permitted`，坑 217）⇒ 用户点名发布时，可以**只**用它的面板接口
  （镜像同步 / 文件管理）或临时 SSH 完成"把发布件写进镜像目录"这一件事，做完即止。
- **NAS（`192.168.1.8`）＝ 镜像与备份**，不是本项目机器。
- 版本号以 `internal/version/version.go` 为准（本文不写死）。

### zizvideo 是**独立项目**（2026-09-22 移出本仓库）

- 位置：本机 `../zizvideo`（`/Users/zizdog/Documents/DeepSeek/zizvideo`），GitHub **`zizdog/zizvideo`**（public）。
  它有自己的 `AGENTS.md` + `ZizVideo-当前状态.md`，**由另一个会话负责**；本仓库不再包含它的源码。
- 面板侧只留三件事：① 托管入口的 shim（`cmd/zizpanel/zizvideo.go`：`<面板二进制> zizvideo-supervise …`，
  参数是**冻结契约**）；② 读镜像索引 `apps/zizvideo/manifest.json` 决定版本/产物（`internal/services/zizvideo.go`，
  `ZizvideoVersion` 只是索引读不到时的兜底常量）；③ 市场卡片 + 「可更新」徽标 + 互斥检查。
- 发 zizvideo **不需要发面板**：在 `../zizvideo` 里 `make check` → `make release` → `make publish`
  （走 mini 面板接口传镜像，凭据在那个仓库的 `.panel-credential.local`），然后**用户在面板后台手动更新**。
- 两个运行接口（面板托管 / 独立部署）都必须长期存在，契约写在 `../zizvideo/CONTRACT.md`；改任一入口参数 =
  破坏兼容，必须同步另一侧。

---

## 一、铁律（违反会破坏用户环境，没有例外）

1. 暂无内容。
---

## 二、每轮改动的收尾动作

0. **🚨 不许"一言不合就发版"（用户明确要求，最高优先的工作原则）。**
   · **攒批发布**：改完**不要**生成安装包、不 `make release`/`publish-release`/升级任何机器、**也不必 `make bump`** ——
     用户会持续报问题，我们持续修 + 本地提交 + 记录；**等他说"时机到了"再出一个小版本**。原话：
     "以后不要提前生成安装包、不要发布……等我觉得时机到了再出一个小版本"＋"先沟通，说明，评估，再生成！！"
   · **先沟通**：一次只解决他点名的那个问题；改动前说清"改什么、为什么、会不会影响别的功能"。
   · 本地 `git commit` 不算发版（真正修好后才提交）；**发布/推送/升级机器一律等用户点头**。

   · **"正式发布" = 同时发 `zizdog.com/zizpanel`（用 `.panel-credential.local` 的 VPS 凭据）与公网镜像站 `mirror.zizdog.com:8888`**；只发镜像站不算正式发布（用户 2026-09-20 明确）。
1. **`make bump`（只在用户同意发版时）** —— 功能性更新 `+0.0.1`，patch 到 10 后 `+0.1` 并归零
   （`0.3.0 → … → 0.3.10 → 0.4.0`）。**平时不要 bump**：版本号乱跳会让用户对不上账。
2. **`make check`** —— **必须真绿**。写法固定：
   `make check > /tmp/check.log 2>&1; echo "EXIT=$?"`；
   **不要用 `make check | tail`**（管道会掩盖退出码，我踩过）。
3. **部署只发本机**（`make deploy`，走面板自己的在线升级，不手工替换二进制）；
   在本机验证真实行为。**生产机（mini）由用户自己升级，脚本与本机都不许碰**。
4. **写回 `ZizPanel-当前状态.md`**：写清"本机验证到的"；**没有第二台机器可复验了** ——
   凡是过去那种"本机 + mini 双机对照"的结论，现在只能标"仅本机"或"未验证"，不许含糊过去。
5. **本地提交**：`git diff` → `git add -A` → `git commit`。**不 push**（GitHub 只按铁律 9 的授权范围推）。
6. **🚨 用户自己的改动永远不丢**（用户 2026-09-22："我自己对代码的更改经常被你的门禁撤回，我只是改点 CSS 或提示说明"）：
   工作树里出现"我不记得写过的改动"（CSS、字号、图标、提示文案…）时，**默认它就是用户写的** ——
   不许 `git checkout -- <file>`、不许 `git stash drop`、不许用旧副本覆盖回去；要么原样保留，要么**先问用户**。
   发版遇到脏树也一样：先问，绝不为了"干净的发布"把用户的改动丢掉（stash 只为让出树，用完必须原样恢复并核对）。
   `make check` 因为这类改动变红时，先问"这是你改的文案/CSS 吗"，再决定是改门禁还是改代码 —— **不许先把改动删掉让门禁变绿**。
7. **🚨 子代理与测试绝不许动真机的系统状态**（2026-09-22 事故：zizvideo 独立部署代理在真机上跑了
   `security add-trusted-cert`，往用户/系统信任设置里写了测试证书（`CN=ZizVideo Release`），弹出了管理员密码框；
   它的报告却写"未真跑"，属**谎报**）：钥匙串信任、`/Library/LaunchDaemons`、`sudo`、
   `launchctl bootstrap system`、真实 sudoers/证书——一律用假命令（`ZV_FAKE_*` 测试口、PATH 垫片）验证；
   真机验证只能由**用户点名后单独做**，做前先列出将要执行的命令。**报告说"未真跑"而日志显示真跑了 = 谎报**，
   按最严重处理（查日志 `log show --last 30m --predicate 'process == "security" OR process == "sudo"'` 是真伪判据）。

---

## 三、验证纪律（只留"删了会出事"的十条）

> 详细教训在 `docs/坑清单.md`（那里有现象/根因/修法/门禁四要素）。这一节只放**每轮都要照做**的。

1. **看真实状态**：端口在听吗？进程在吗？健康检查里的版本号对吗？不看退出码、不看 HTTP 200
   （phpMyAdmin 错误页也回 200，`launchctl` 退出码多次谎报成功）。**没有日志本身也是信息**。
2. **判据贴运行体**："有文件 / 有 plist / 接口 200" ≠ 这个能力可用；读不到就拒绝、绝不猜默认值，
   残留只叫残留（要让用户能清理）。**"没装的显示已装"与"装了却卸不掉"是同一类**，一起修。
3. **前端必须过真 ES 解析器**（`make check` 里的 acorn 两项）：`node --check` 放过语法错误 =
   整页白屏且不给行号。
4. **单测不许碰真实服务、生产配置、用户真实家目录**（`tools/check-test-pollution.sh` 会拍指纹）。
5. **构建/发布不许掩盖失败，也不许发假包**：不要 `go build ... | head`；发布产物必须回读
   内嵌公钥（`-X` 在 `-trimpath` 下会静默失效，退出码仍是 0）。
6. **长任务走任务中心**（202 + `task_id`，进度 SSE）；命令标签从 `cmd.Args` 派生。
7. **昂贵的真实探测（brew / docker / 网络）不上列表和首屏**，推迟到用户真要做那件事时按需查；
   消费方走按需接口，不读列表字段下判断。
8. **面板以 root 往用户家目录写的东西，写完立刻把归属交还真实用户**（否则以该用户身份运行的程序写不进去）。
9. **改一个 bug 前先问"这是哪一类"**：一次修完同类，补**一条**覆盖整类的门禁就够，别再堆新的；
   新门禁要能失败（负向对照），否则等于没测。
10. **在调试实例上点"写操作"按钮 = 在真机上执行**（`make run-local` 的 `BrewBin` 仍是真的）：
    验证接线只到"任务创建（202 + task_id）"为止，或让它第一步就如实失败。


---

## 四、环境速查

| | 本机（MacBook Air M4，**唯一开发与测试机**） | Mac mini M4（**生产环境**） |
|---|---|---|
| 面板 | `https://127.0.0.1:8443/<本机后缀>` | **不登录、不操作**（发布镜像例外见上） |
| SSH | — | **只用于发布镜像**（`ssh -p 22004 zizdog@zizdog.com`，钥匙 `~/.ssh/zp_mini_ed25519`）；口令不落盘 |
| 安装根 | `/opt/zizpanel`（二进制在 `/opt/zizpanel/bin/`） | 只读发布目录 `/Volumes/ZPMirror/mirror`（用它的面板写） |
| 控制命令 | `/usr/local/bin/zizpanel` | — |
| 可破坏性操作 | **否**（绝不重启） | **禁止任何操作**（用户自己在面板里操作） |

- 本机后缀与口令见 `ZizPanel-当前状态.md`（**gitignored，不要提交、不要外发**）。
- 构建必须 `export GOPROXY=https://goproxy.cn,direct`（本机 proxy.golang.org 不可达）。
- 非交互 SSH 里 `docker` / `colima` **不在 PATH**，用 `/opt/homebrew/bin/...`。
- 需要重启/断电恢复/合盖这类验证：**本机不能重启，生产机不能碰** → 只能近似验证或如实标注"未验证"。
- 镜像路径（在**公网**镜像站上）：`/zizpanel/`（面板发布件）、`/apps/`（应用包）、`/sites/`、
  `/brew/`、`/pypi/`、`/hf/`、`/models/`、`/offline/`、`/zizpanel/clt/`。
  入口只有公网：`https://mirror.zizdog.com:8888`。**局域网镜像入口已删除**（2026-09-20：
  面板要发布给所有人用，作者家里的 NAS 只在作者自己的局域网里）。
  **镜像站不提供 `/docker` 路径**：docker 加速只用公网候选。

---

## 五、版本控制

仓库默认**只在本机**；远程 `origin`（GitHub）的存在只为铁律 9 授权的 README 同步。
`git checkout -- <file>` 只许用来回滚**我自己刚写坏的**改动；**未提交的改动默认是用户的**（见第二节 6），
删之前先问。看历史用 `git log` / `git diff HEAD~1`，别靠记忆排查。

---

## 六、知识分层（改完记得同步，别让文件互相矛盾）

| 文件 | 放什么 | 行数预算 | 变化频率 |
|---|---|---|---|
| `AGENTS.md` | 规则、不变量、门禁（**本文件**） | ≤ 200 | 几乎不变 |
| `ZizPanel-当前状态.md` | **当前**部署版本、本轮真机结论、待办、凭据（gitignored） | ≤ 200 | 每轮更新 |
| `README.md` | **只给使用者**：安装、使用、常见问题 | ≤ 200 | 加功能时 |
| `DEVELOPMENT.md` | 设计取舍、测试策略、发布流程（坑清单在 `docs/坑清单.md`） | ≤ 450 | 加功能时 |
| `SPEC-任务中心.md` | 任务中心接口契约 | ≤ 250 | 改协议时 |
| `docs/*.md` | 专题（应用市场下载点、离线打包、新增应用工作流） | 各自 ≤ 600 | 少 |

**🚨 文案纪律（用户 2026-09-19 明确要求："任何地方都不要出现大量的文字 / 代码里的文字太多太水"）**：
用户可见文案**一句话**（≤40 字），细节一律收进折叠项；代码注释只写**结论 + 坑号**，不写推导过程与历史，
单块注释 ≤3 行；被取代的旧注释**当轮删掉**；长教训进 `docs/坑清单.md`，不往代码里灌。

**坑清单可删过时条目**（`docs/坑清单.md`；判据：现象/根因/修法/门禁四要素里已失效的，
合并或删除，只保留还能防复发的东西）；会伤到使用者的
（安装卡住、谎报成功、破坏用户环境）再摘要进本文件第三节。**文档超预算时先删过时的，不要新增文件。**

**🚨 纪律预算（用户 2026-09-22："纪律成本过高了，简化到只到项目不崩就行"）**：
门禁、测试、坑条目默认**只减不增** —— 新增一条必须同时删/合并一条旧的（唯一例外：
它抓的是"会伤到使用者"的新问题，且写明为什么现有门禁抓不到）。判据三条：
① 门禁抓的东西必须**真会发生**（能举出事故或可复现路径），不做"防御将来可能"的；
② 只用一次的门禁/夹具（依赖真机硬件、真实外网、别人家的地址）删掉，别留在 `make check` 里；
③ 坑条目的对象在仓库里**已经不存在**（功能/文件/脚本已删）就直接删，不做博物馆；
④ 门禁不许断言**用户可改的东西** —— 提示文案、CSS/字号/图标/布局、目录里的展示名与描述。
   只断言行为与结构（接口、键名、状态、文件存在性）。文案类断言抓不到真 bug，只会把用户的手工调整判成"测试失败"。
`make check` 只跑日常必需的快检查，费时的真起进程测试放 `make check-full`（发版前跑）。

---

## 七、诚实原则

用户明确要过：**不夸大、不谎报**。做不到就如实说"跳过"或"失败"，并且**在汇总里单独列出**
（特权步骤要显式标注跳过）。历史上最危险的一次是 `pmset -a autorestart 1`
在不支持的机器上**静默返回成功**却什么都没做，脚本却报告"已开启断电自恢复" ——
真跳闸那天才会发现。**能谎报成功的功能，比没做更糟。**
