# ZizPanel — 工作区规则

> **本文件由 DSH 在每个新会话自动加载**（`AGENTS.md` 约定），所以它只放**不变的规则**。
> 开工前请先读 **`ZizPanel-当前状态.md`**（进度/待办/残留，含口令，已 gitignore），
> 需要细节再读 **`README.md`**（面向使用者的完整说明 + 坑清单）。
>
> 把规则放在这里、而不是靠每次手动贴交接文档，是因为**新会话最容易重犯已经修过的错**。

## 项目一句话

只做 macOS 的类宝塔面板：Go 单二进制 + 内嵌原生 ESM 前端（**无构建步骤**）+ SQLite，
一条 `curl | sudo bash` 装完即可远程网页操作。当前本机 **v0.4.1**（Mac mini 停在 0.3.5，
局域网关闭期间**不部署** mini）。

---

## 一、铁律（违反会破坏用户环境，没有例外）

1. **只做 macOS。** 不追求跨平台，可以放心用 `launchctl` / `pmset` / `diskutil` 这类专属能力。
2. **绝不动 `~/www/zizdog.cn`。** 那是另一个项目（TtsVoice）。面板只负责它的 nginx vhost
   与站点注册记录，**不改它的代码、配置、数据库**。
3. **不改 Typecho 核心**（`var/`、`admin/`、`index.php`、`config.inc.php`）
   与**底层环境**（nginx 全局配置、系统 PHP/MySQL 的既有约定）。
4. **绝不重启本机（MacBook Air）。** DSH 会话跑在它上面，一重启就断。
   **需要重启才能验证的事，去 Mac mini 上做**（mini 已验证开机自启，随便重启）。
5. **局域网 `192.168.1.8` 是 `fnnas`（NAS），不是本项目机器** —— 不要连、不要动。
6. **破坏性操作前先快照**，并且先用 `git diff` 看清将要发生什么。
7. **不要把任何口令写进仓库。** 真实面板口令放 `.panel-credential.local`（已 gitignore）；
   `.release-key/`、`dist/`、`node_modules/`、`ZizPanel-当前状态.md` 同样不进仓库。

---

## 二、每轮改动的收尾动作（用户明确要求）

> ⚠️ **2026-09-14 起：局域网已关，只在本机开发与验证。**
> 下面第 3、4 条暂时改为「只在本机」——**不要**再连/部署/验证 mini
> （`192.168.1.4` 已不可达，连它只会浪费时间）。等用户明确说局域网恢复，
> 再回到「两机部署 + 在 mini 上验证」。
> 最后已知的 mini 状态：**panel 0.3.5、receiver 1.3.0**（2026-09-14 07:14，同步完成）。

1. **`make bump`** —— 版本规则：每次功能更新 `+0.0.1`，patch 到 10 后 `+0.1` 并把 patch 归零。
   `0.3.0 → 0.3.1 → … → 0.3.10 → 0.4.0`。
   理由：两台机器同时在跑，需要足够细的粒度才能一眼看出哪台旧了。
2. **`make check`** —— **必须真绿**（`gofmt` + shell 变量扫描 + `go vet` + 全部单测 +
   JS 语法 + receiver `/jobs` 测试）。
3. **部署到本机**（`https://127.0.0.1:8443`，走面板的在线升级），并**在本机验证**真实行为。
   本机具备完整闭环：网站 `zizdog.cn` 就在本机，`127.0.0.1:8880` 上有 TtsVoice 的 Qwen
   （3 个模型，含 0.6B Base / CustomVoice），需要真实合成时用它。
4. 把结论写回 `ZizPanel-当前状态.md`——**只写本机验证到的结论**，
   不要凭上一轮的记忆声称 mini 也通过了。

---

## 三、验证纪律（这个项目最贵的教训，反复复发）

- **看真实状态，不看退出码、不看 HTTP 200。**
  端口在听吗？进程在吗？健康检查里的**版本号**对吗？
  历史上：测试打印过"✓ 自启路径成立"，但被测的引擎**根本没停过**；
  phpMyAdmin 的错误页也**返回 200**；`launchctl` 的退出码**多次谎报成功**。
- **"没有日志"本身就是信息。** 面板起来后一行日志都没有 →
  失败发生在日志初始化**之前**，去查配置/目录/证书/文件归属，别在业务逻辑里找。
- **"校验通过"取决于用谁校验。** 前端语法必须用真正的 ES 解析器
  （`tools/check-js-syntax.mjs`，走 acorn）。`node --check` 曾放过浏览器拒绝的语法，
  结果是**整页白屏**且控制台不给文件名、不给行号。
- **`go build ... | head` 会掩盖失败的退出码** —— 不要这么写。
- **`-X` 在 `-trimpath` 下会静默失效**（符号被死代码消除，退出码仍是 0）。
  所以 `make release` 必须**运行产物核对公钥**。
- **单测不许碰真实服务。** 本机 8880 上有 TtsVoice 的 Qwen（3 个模型），
  测试要用 `Manager.qwenPortOverride` 之类的手段隔离。
- **测试不许碰生产配置。** 涉及 nginx vhost 的测试必须沙箱化，
  并断言**生产的 `000-default.conf` 一字未变**（曾因为漏沙箱化把生产配置改坏，`/_panel` 全 502）。
- **长任务必须走任务中心，不许再写成同步请求。** 安装/卸载/部署动辄几分钟，
  同步请求有三个后果：用户只能看"请等待"、关掉窗口就再也找不回进度、
  而且任务挂在 `r.Context()` 上 —— **用户一刷新就把 brew/docker 杀了**，
  机器上留下装到一半的状态。现在这类操作一律 `launchTask(...)` 返回 202 + `task_id`，
  进度走 SSE（见 `SPEC-任务中心.md`）。秒级动作（如 `compose stop`）仍然同步返回。
- **任务执行的命令标签必须从 `cmd.Args` 派生。** 手写标签会和实际执行的不一致
  （`runColima` 漏过 `sudo -n -u <user>`，日志显示成没降权的直接调用，用户照抄就是另一回事）。
- **测试不许碰用户真实家目录。** `config.Default()` 里的 `UserHome`/`WWWRoot`
  解析的是**真实**家目录，测试服务器必须把它们指向 `t.TempDir()`。
  2026-09-14 事故：`newTestServer` 漏隔离 `UserHome`，`make check` 把用户真实的
  `~/Library/LaunchAgents/sh.brew.*.plist` 覆盖成了空 plist —— 服务当时在跑所以
  **零症状**，但只要重启或点一次"重启服务"就会永久起不来。现在有两道门禁：
  护栏测试 `TestTestServerSandboxedAwayFromRealHome` +
  `make check` 里的 `tools/check-test-pollution.sh`（跑测试前后给真实目录拍指纹）。

---

## 四、环境速查

> ⚠️ **mini 当前不可达**（局域网已关）。下表第 3 列仅作记录，
> 不要尝试连接；要重启才能验证的事现在也只能在本机想办法绕开。

| | 本机（MacBook Air M4，开发机） | Mac mini M4（服务器，⚠️ 暂不可达） |
|---|---|---|
| 地址 | `https://127.0.0.1:8443`、`http://localhost/_panel` | `https://192.168.1.4:8443` |
| SSH | — | `ssh zizdog@192.168.1.4`（局域网关闭期间不可用） |
| 安装根 | `/opt/zizpanel`（二进制在 `/opt/zizpanel/bin/`） | 同左 |
| 控制命令 | `/usr/local/bin/zizpanel` | 同左 |

- 账号与口令见 `ZizPanel-当前状态.md`（**gitignored，不要提交、不要外发**）。
- **非交互 SSH 里 `docker` / `colima` 不在 PATH** —— 用 `/opt/homebrew/bin/...`。
  （这正是面板代码里必须显式注入 PATH 的原因，见 `internal/services/colima.go`。）
- 本机 8880 上的 Qwen 属于 **TtsVoice 项目**（用户级 LaunchAgent）。面板只是"仅纳管"它，
  **不要在面板里对它点"安装"**。

---

## 五、版本控制

仓库已 `git init`。每轮改动：

```bash
git diff                 # 先看清楚自己改了什么
git add -A && git commit -m "一句话说明这一轮做了什么"
```

出错时**不要靠记忆排查**：`git checkout -- <file>` 回滚单个文件，
`git log` / `git diff HEAD~1` 看上一轮改了什么。

> 这项是"新窗口越做越乱"的根因之一：没有版本控制时，
> 每个新会话都在一个说不清状态的代码库上继续加东西，错误只增不减、无法回退。

---

## 六、知识分层（改完记得同步，别让文件互相矛盾）

| 文件 | 放什么 | 变化频率 |
|---|---|---|
| `AGENTS.md` | 规则、不变量、门禁（**本文件**） | 几乎不变 |
| `ZizPanel-当前状态.md` | 进度、待办、残留、真机验证结论（**每轮更新**） | 每次改动 |
| `README.md` | 面向使用者的完整说明 + 已修复坑清单 | 加功能时 |
| `Mac-mini部署指南.md` | 部署流程、服务器模式、SSH/Tailscale | 少 |

**规律：坑清单只增不减。** 修掉一个新坑，就把它追加到 `README.md` 的
「已修复的典型坑（有测试锁死）」一节；那条坑如果会伤到使用者，再摘要进本文件第三节。

---

## 七、诚实原则

用户明确要过：**不夸大、不谎报**。做不到就如实说"跳过"或"失败"，
并且**在汇总里单独列出来**（`make smoke` 就是这么处理特权步骤的）。
历史上最危险的一次是 `pmset -a autorestart 1` 在不支持的机器上
**静默返回成功**却什么都没做，脚本却报告"已开启断电自恢复" ——
真跳闸那天才会发现。**能谎报成功的功能，比没做更糟。**
