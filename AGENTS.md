# ZizPanel — 工作区规则

> DSH 每个新会话自动加载本文件，所以**只放不变的规则**。
> 开工前先读 **`ZizPanel-当前状态.md`**（当前进度/待办/真机结论/凭据，已 gitignore）；
> 要细节再读 **`DEVELOPMENT.md`**（设计取舍、测试策略、发布流程）；坑清单见 **`docs/坑清单.md`**。
> **`README.md` 只给使用者看**，别再往里塞开发过程。
>
> 文档存在的意义是**少占上下文**：每份文件都有行数预算（见第六节）。
> 历史过程不进文档 —— 进 git 历史，或压成 `docs/坑清单.md` 里的一条结论。

## 项目一句话

只做 macOS 的类宝塔面板：Go 单二进制 + 内嵌原生 ESM 前端（**无构建步骤**）+ SQLite，
一条 `curl | sudo bash` 装完即可远程网页操作。

- **本机（MacBook Air M4）＝ 唯一的开发机与测试机**：`https://127.0.0.1:8443/<后缀>`。
- **🛑 Mac mini M4 ＝ 生产环境（2026-09-17 起）**：**严禁任何接触** —— 不 SSH、不部署、不跑测试、
  不做破坏性验证、不读它的站点/数据库/配置/凭据。本机**不保存它的访问凭据**（系统/面板口令、SSH 主机记录与密钥、
  面板后缀、SSH 入口）；历史文档与测试夹具里可能仍有它的私网地址，**那只是历史记录，不得据此去访问**。
- **NAS（`192.168.1.8`）＝ 镜像与备份**，不是本项目机器。
- 版本号以 `internal/version/version.go` 为准（本文不写死）。

---

## 一、铁律（违反会破坏用户环境，没有例外）

1. 暂无内容。
---

## 二、每轮改动的收尾动作

0. **🚨 不许"一言不合就发版"（用户明确要求，最高优先的工作原则）。**
   · **攒批发布**：改完**不要**生成安装包、不 `make release`/`publish-release`/升级任何机器、**也不必 `make bump`** ——
     用户会持续报问题，我们持续修 + 本地提交 + 记录；**等他说"时机到了"再出一个小版本**。原话：
     "以后不要提前生成安装包、不要发布……等我觉得时机到了再出一个小版本"＋"先沟通，说明，评估，再生成！！"
   · **先沟通**：一次只解决他点名的那个问题；改动前说清"改什么、为什么、会不会影响别的功能"。
   · 本地 `git commit` 不算发版（真正修好后才提交）；**发布/推送/升级机器一律等用户点头**。

   · **"正式发布" = 同时发 `zizdog.com/zizpanel`（用 `.panel-credential.local` 的 VPS 凭据）与公网镜像站 `mirror.zizdog.com:8888`**；只发镜像站不算正式发布（用户 2026-09-20 明确）。
1. **`make bump`（只在用户同意发版时）** —— 功能性更新 `+0.0.1`，patch 到 10 后 `+0.1` 并归零
   （`0.3.0 → … → 0.3.10 → 0.4.0`）。**平时不要 bump**：版本号乱跳会让用户对不上账。
2. **`make check`** —— **必须真绿**。写法固定：
   `make check > /tmp/check.log 2>&1; echo "EXIT=$?"`；
   **不要用 `make check | tail`**（管道会掩盖退出码，我踩过）。
3. **部署只发本机**（`make deploy`，走面板自己的在线升级，不手工替换二进制）；
   在本机验证真实行为。**生产机（mini）由用户自己升级，脚本与本机都不许碰**。
4. **写回 `ZizPanel-当前状态.md`**：写清"本机验证到的"；**没有第二台机器可复验了** ——
   凡是过去那种"本机 + mini 双机对照"的结论，现在只能标"仅本机"或"未验证"，不许含糊过去。
5. **本地提交**：`git diff` → `git add -A` → `git commit`。**不 push**（GitHub 只按铁律 9 的授权范围推）。

---

## 三、验证纪律（这个项目最贵的教训，反复复发）

- **看真实状态，不看退出码、不看 HTTP 200。** 端口在听吗？进程在吗？健康检查里的
  **版本号**对吗？历史上：测试打印过"✓ 自启路径成立"但被测引擎**根本没停过**；
  phpMyAdmin 的错误页也**返回 200**；`launchctl` 的退出码**多次谎报成功**。
- **"没有日志"本身就是信息。** 面板起来后一行日志都没有 → 失败发生在日志初始化**之前**，
  去查配置/目录/证书/文件归属。
- **"校验通过"取决于用谁校验。** 前端语法必须用真正的 ES 解析器
  （`tools/check-js-syntax.mjs`，走 acorn）。`node --check` 曾放过浏览器拒绝的语法，
  结果是**整页白屏**且控制台不给文件名、不给行号。
- **`go build ... | head` 会掩盖失败的退出码**，不要这么写。
- **`-X` 在 `-trimpath` 下会静默失效**（退出码仍是 0），所以 `make release`
  必须**运行产物核对公钥**。
- **单测不许碰真实服务、生产配置、用户真实家目录。** 门禁：`TestTestServerSandboxedAwayFromRealHome`
  + `tools/check-test-pollution.sh`（跑测试前后给真实目录拍指纹）。本机 8880 上有真实 Qwen，要用
  `Manager.qwenPortOverride` 之类手段隔离；涉及 vhost 的测试必须沙箱化并断言生产 `000-default.conf` 一字未变。
- **长任务必须走任务中心**（`launchTask(...)` → 202 + `task_id`，进度走 SSE）。
  同步请求会让用户只能看"请等待"、关掉窗口就找不回进度，而且任务挂在 `r.Context()` 上 ——
  **用户一刷新就把 brew/docker 杀了**。秒级动作（如 `compose stop`）仍可同步返回。
- **任务执行的命令标签必须从 `cmd.Args` 派生**，手写标签会和实际执行的不一致。
- **手工调面板 API 的三个坑**：基址必须带**面板后缀**；写操作要带 `X-CSRF-Token`
  （值取可读的 `zp_csrf` cookie，双提交模式），否则 403；`GET /api/v1/services`
  的键是 **`list`**，不是 `services`。
- **任何"看起来不对"的结论，先怀疑自己的探测方式**（键名写错、文件后缀猜错都真的发生过），
  再去怀疑对方。
- **修一个 bug 之前先问"这是哪一类问题"。** 用户点名的例子只是样本：先找出同一类里的
  **所有**实例、一次修完，再补一条**覆盖整类**的门禁（遍历全目录的不变量测试 / 状态→动作矩阵 /
  用户可见文案 grep）。只修被点名的那一个，下一次就是同样的报障 —— 历史反复证明过：
  「卸载入口缺失」修了三轮，「有文件就算已安装」修了 Colima 还有别的条目。
  **"没装的显示已装"与"装了却卸不掉"属于同一类**：判据必须来自运行体/真实证据，
  而且"能装"必须配"能卸"（`installed=true` ⇒ 必须有可用的卸载路径，有测试锁死）。
- **面板以 root 往用户家目录写东西，必须把归属交还真实用户**（否则以该用户身份运行的程序写不进去：
  nginx 日志目录与 Colima 配置都归坑 163，表现为 `colima start` 报 permission denied 而包其实已装好）。
  判据是"写完之后 chown 回真实用户"，不是"下次再说"。
- **"有产物/有配置文件/接口 200" ≠ "这个能力可用"。** 判据要贴着该能力的**运行体**，读不到就拒绝、
  绝不猜默认值：僵尸 plist 让市场谎报已装（坑 161）；服务登记表里没有 ⇒ 谎报未就绪（坑 170/174）；
  `nginx.conf` 没有 `user` ⇒ 猜个 `nobody` chown 上去 ⇒ 大请求体一律 500（坑 173）；
  `/v1/models` 200 而合成 100% 失败（坑 176）。残留只能叫"残留"，要单独报出来让用户能清理/重装。
- **默认值要能用，且每组上限都要有面板入口。** 用户不该先撞墙（phpMyAdmin 导入 413）
  才知道默认值不能改；"保存成功"也不等于"已生效" —— 必须回读生效值，读不到就如实说"未复核"（坑 164）。
- **昂贵的真实探测（brew / docker / 网络）不许放在列表或首屏路径上**：推迟到用户真要做那件事时
  按需查（按钮上要有可见进度）。分层的代价是"列表结论不完整"，所以消费方必须走按需接口，
  不能读列表字段下判断（读了不报错，只会悄悄少一个按钮 —— 有测试遍历全 JS 锁死；坑 165）。
- **发布纪律**：发布只提交**显式列出**的文件（有并行编辑者时禁用 `git add -A`），发布后核对产物里
  **有该有的符号、没有不该有的**（坑 177）。
- **在调试实例上点"写操作"按钮 = 在真机上执行。** `make run-local` 只临时化了配置与数据目录，
  `BrewBin` 仍是真机 `/opt/homebrew` —— 曾因此真的跑起 `brew install colima`（坑 162）。
  验证"按钮接线"只到**任务创建**（202 + task_id 可查）为止，或先把 `BrewBin` 指向不存在的路径
  让它在第一步就如实失败。

---

## 四、环境速查

| | 本机（MacBook Air M4，**唯一开发与测试机**） | Mac mini M4（**生产环境**） |
|---|---|---|
| 面板 | `https://127.0.0.1:8443/<本机后缀>` | **不记录、不访问** |
| SSH | — | **禁止**（本机已删除其主机记录与全部凭据） |
| 安装根 | `/opt/zizpanel`（二进制在 `/opt/zizpanel/bin/`） | — |
| 控制命令 | `/usr/local/bin/zizpanel` | — |
| 可破坏性操作 | **否**（绝不重启） | **禁止任何操作**（用户自己在面板里操作） |

- 本机后缀与口令见 `ZizPanel-当前状态.md`（**gitignored，不要提交、不要外发**）。
- 构建必须 `export GOPROXY=https://goproxy.cn,direct`（本机 proxy.golang.org 不可达）。
- 非交互 SSH 里 `docker` / `colima` **不在 PATH**，用 `/opt/homebrew/bin/...`。
- 需要重启/断电恢复/合盖这类验证：**本机不能重启，生产机不能碰** → 只能近似验证或如实标注"未验证"。
- 镜像路径（在**公网**镜像站上）：`/zizpanel/`（面板发布件）、`/apps/`（应用包）、`/sites/`、
  `/brew/`、`/pypi/`、`/hf/`、`/models/`、`/offline/`、`/zizpanel/clt/`。
  入口只有公网：`https://mirror.zizdog.com:8888`。**局域网镜像入口已删除**（2026-09-20：
  面板要发布给所有人用，作者家里的 NAS 只在作者自己的局域网里）。
  **镜像站不提供 `/docker` 路径**：docker 加速只用公网候选。

---

## 五、版本控制

仓库默认**只在本机**；远程 `origin`（GitHub）的存在只为铁律 9 授权的 README 同步。
出错时不要靠记忆排查：`git checkout -- <file>` 回滚单个文件，`git log` / `git diff HEAD~1` 看上一轮改了什么。

---

## 六、知识分层（改完记得同步，别让文件互相矛盾）

| 文件 | 放什么 | 行数预算 | 变化频率 |
|---|---|---|---|
| `AGENTS.md` | 规则、不变量、门禁（**本文件**） | ≤ 200 | 几乎不变 |
| `ZizPanel-当前状态.md` | **当前**部署版本、本轮真机结论、待办、凭据（gitignored） | ≤ 200 | 每轮更新 |
| `README.md` | **只给使用者**：安装、使用、常见问题 | ≤ 200 | 加功能时 |
| `DEVELOPMENT.md` | 设计取舍、测试策略、发布流程（坑清单在 `docs/坑清单.md`） | ≤ 450 | 加功能时 |
| `SPEC-任务中心.md` | 任务中心接口契约 | ≤ 250 | 改协议时 |
| `docs/*.md` | 专题（应用市场下载点、离线打包、新增应用工作流） | 各自 ≤ 600 | 少 |

**🚨 文案纪律（用户 2026-09-19 明确要求："任何地方都不要出现大量的文字 / 代码里的文字太多太水"）**：
用户可见文案**一句话**（≤40 字），细节一律收进折叠项；代码注释只写**结论 + 坑号**，不写推导过程与历史，
单块注释 ≤3 行；被取代的旧注释**当轮删掉**；长教训进 `docs/坑清单.md`，不往代码里灌。

**坑清单可删过时条目**（`docs/坑清单.md`；判据：现象/根因/修法/门禁四要素里已失效的，
合并或删除，只保留还能防复发的东西）；会伤到使用者的
（安装卡住、谎报成功、破坏用户环境）再摘要进本文件第三节。**文档超预算时先删过时的，不要新增文件。**

---

## 七、诚实原则

用户明确要过：**不夸大、不谎报**。做不到就如实说"跳过"或"失败"，并且**在汇总里单独列出**
（`make smoke` 就是这么处理特权步骤的）。历史上最危险的一次是 `pmset -a autorestart 1`
在不支持的机器上**静默返回成功**却什么都没做，脚本却报告"已开启断电自恢复" ——
真跳闸那天才会发现。**能谎报成功的功能，比没做更糟。**
