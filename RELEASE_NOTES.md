v1.1.7 · Docker 一键安装 + 去掉「纳管」这套内部词 + 应用描述只说一两句

**Docker 版块：没装运行时就直接给一键安装。** 以前它只说"环境不可用"，然后把用户指去
「应用市场」和「服务管理」两个入口 —— 前者很难找，后者早就并进应用市场了。现在这一页直接给
一颗「🐳 一键安装 Docker（Colima）」，并如实写清代价（会装 Colima + Lima + Docker 命令行 +
docker-compose，下载约 1–3 GB；随后创建并启动 Linux 虚拟机，首次启动约 40 秒；装完自动配置
开机自启），进度和失败原因都在「任务中心」里看。如果运行时已经装上、只是引擎没在跑，按钮会
变成「▶ 启动 Docker 运行时」。

**「已安装」不再被残留文件骗。** 之前只要 `/Library/LaunchDaemons/` 里留着一份旧的
`com.zizdog.colima.plist`，面板就认为 Colima"已安装"—— 于是卡片显示「Colima 已安装·未纳管」，
可机器上连二进制都没有，引擎根本起不来。现在容器运行时的判据贴着它的**运行体**：
二进制在不在、socket 通不通、Docker API 答不答（三态：没装 / 装了没跑 / 正在运行）。
只有残留（旧开机自启配置、旧配置目录）时会如实写成"未安装 + 检测到上次安装的残留，可重新安装"。

**不再让用户理解「纳管」。** 这是面板自己的事：

- 删掉「🔍 扫描可纳管服务」入口（面板启动时会自动登记本机已有的、它认识的服务）；
- 市场卡片上的「纳管」→ **「添加到面板」**（只把已有服务接进来，不重装）；「已纳管」→「已安装」；
- 管理面板里的「取消纳管（仅删除记录）」→ **「从列表移除（不卸载软件）」**，确认框逐字写明
  面板不会跑 `brew uninstall`、不会删任何文件，软件在系统里的运行与配置完全不受影响；
- Docker 页的「已纳管服务」分区一并去掉。

**状态筛选更清楚。** 原来的「异常」与「仅健康检查失败」是包含关系（后者是前者的子集），
现在合并成四项：**全部 / 运行中 / 已停止 / 需要处理**。工具条上那颗提示按钮也改成
「⚠ N 个需要处理」，点进去正好筛出这 N 张卡片 —— 原来的两套判据命中的卡片**一张都不会漏**。

**应用描述只说一两句。** 36 个应用的说明全部压到 ≤80 字（最长一句 56 字）；需要留档的设计
理由与历史教训挪进了代码注释。以后新增应用时门禁会按这个上限卡住。

**升级方式**：面板「面板设置 → 检查更新」→ 一键更新。
