v1.0.0 · 第一个正式版：安装会问人了，国内下载统一走镜像

**安装脚本现在真的会问人**（有终端时）：管理员用户名、登录口令（不回显）、面板路径后缀、
监听端口；在**本机**直接安装时还会问「是否开启 SSH」（通过 SSH 从别的电脑连过来安装时不问
—— 远程访问本来就通着）。口令只走交互输入或 `ZP_PASS`，**不进命令行参数、不进日志**。

**没有终端也能装成功**：`curl … | sudo bash`（stdin 是管道）没有终端可问，脚本会
**自动生成一个随机初始口令并在结果里显著打印一次**；也可以 `ZP_USER` / `ZP_PASS` /
`ZP_SUFFIX` / `ZP_PORT` 一起给，全程不提问。所有 `read` 都是"能读就读、读不到用默认"，
绝不在管道里卡住。

**安装完会问「是否开启免授权访问内网段」**（可选、默认关）：选"是"时用面板已有的接口写入，
并逐字说清三件事 —— ① 需要重启电脑才生效；② **不必现在重启**，面板的 Plan B（回环转发器）
已经能让局域网反代正常工作；③ 以后也能在面板「系统设置」里手动打开。

**国内下载体验**：镜像地址集中定义在 `install.sh` 顶部（brew / CLT / 应用包 / 发布件同一处），
一律"镜像优先 + 探不通才回落官方"。面板发布件与在线升级源改走**公网 `https://zizdog.com/zizpanel`**
（任何网络都能用），NAS 只作局域网加速/回落；Homebrew 安装脚本走中科大镜像（实测 200/0.18s），
bottles 走 NAS → 中科大 → 清华 → 阿里云，CLT 探到镜像就把基址写进 launchd 环境变量。
安装脚本写 `config.json` 时会一并写入 `mirror_base` 与 `upgrade_source`
（后者默认 `https://zizdog.com/zizpanel`，且以"清单 + 签名都在"为可用判据）。

**新增 `--dry-run`**：把整条安装计划（含 SSH 提问与内网预授权提示）在不碰系统的前提下演练一遍。

**修复**：安装脚本预写 `config.json` 后，磁盘上那份长期缺默认字段（`www_root` / `access_mode`
读不到）—— 新增 `zizpanel reconcile-config` 把配置补齐并落盘；`verify_running` 在 `logs/`
不存在时会刷 30 行 `No such file or directory`，现在先建目录、写不进去就只打印不刷屏。

**版本 1.0.0**：递增规则不变（修订号 +1，到 10 进位归零：1.0.0 → … → 1.0.10 → 1.1.0）。

---

v0.11.7 · 证书能被 nginx 读到了（站点 SSL 绑定不再"写好配置却起不来"）

- 修掉一个**只在"nginx 以普通用户运行"的机器上出现**的问题：面板以 root 写出的 ACME
  证书目录是 0700、私钥 0600，homebrew 的 nginx（LaunchAgent，普通用户）读不到，
  `nginx -t` 直接报 `cannot load certificate ... Permission denied`，于是配置写好了、
  重载也"成功"了，但 443 根本没起来。现在证书落盘后会把属主对齐面板数据目录
  （**私钥权限不放宽**，仍是 0600）。
- 说明：这个坑的开发机上一直没暴露（那台机器 nginx 是 root 起的）。

v0.11.6 · dns-01 不再被「泛解析 CNAME」坑死；证书申请进度在任务窗口可见

- 域名有泛解析（`* CNAME 某主机`）时，申请证书会一直失败并报
  `No TXT record found at _acme-challenge.<域名>`：签发引擎默认会把校验记录写到
  CNAME 的目标上，而 Let's Encrypt **不跟随由泛解析产生的 CNAME**。
  现在默认把校验记录写回挑战名本身（显式记录能覆盖泛解析 CNAME，CA 就能查到）。
- 真正需要 CNAME 委派的用户仍可开启（`Manager.DNS01FollowCNAME`）。
- **证书申请的过程现在会显示在任务窗口**：写入 TXT、等待解析生效、CA 校验结果等，
  以前这些只写进服务器日志，用户只看到一句"申请失败"。

v0.11.5 · 修「DNS 解析不可达导致证书一条都签不出来」

- 某些网络里路由器会通告一个**不可达的 IPv6 DNS**，而签发引擎默认用一个**主机名**
  当作 DNS 服务器（要先解析它），于是每次申请都卡在
  `DNS call error: ... :53: i/o timeout`，连 CA 都请求不到。
  现在 dns-01 预检固定使用可达的 IPv4 公共解析器，不再依赖系统那台可能坏掉的解析器。
- 失败时给出的排查提示也一并保留（凭据/权限/冲突记录/传播时间）。

---

v0.11.4 · 应用市场"个个能装"的闭环 + 面板内证书签发 + 数据库口令可用 + 任务限时输入

**应用市场（本轮重点）**
- 27 个应用的**下载点全部登记**（`internal/services/market_downloads.go`）：每个条目写明上游地址、
  体积、镜像状态；镜像状态是**三态**（已镜像 / 缺失 / 不需要），"不需要"必须给理由。
- 三层保障：静态门禁（`go test` 的一部分，目录改了声明没改就失败）→ 在线审计
  （`make market-audit` 系列）→ 手册（`docs/新增应用工作流.md`，**加一个应用 = 5 步，每步一条命令**）。
- 安装失败的表述改成**如实报错**：镜像上没有就说没有，不再"看起来成功"。

**证书**
- 面板内直接向 Let's Encrypt 签发/续期，支持 HTTP-01 与 DNS-01（通配符必须 DNS-01），
  每天自动检查到期并续期；站点 SSL 启用后会做**真实 403 探针复核**。

**数据库**
- 修掉"改 MySQL 口令永远失败"的真因（旧实现改完口令还用旧口令重连）。
- 口令语句改走标准输入，**不再出现在 `ps` 输出里**。
- 数据库面板新增口令列（打码 / 👁 查看 / 📋 复制）；外部设置的口令如实显示"不可回显"。

**任务中心**
- 安装过程中需要你输入时（例如 MySQL root 口令），任务窗口会弹出**限时输入**；
  超时或留空则**自动生成强随机口令**并在任务结果里给一次性凭据区块，不会让安装卡死。

**离线与迁移**
- NAS 镜像补齐：命令行开发者工具、pip 包、Colima 虚拟机镜像、Ollama 模型、IOPaint 权重。
- 新增"仅走 NAS（离线）"模式与离线打包工具（`docs/离线打包与迁移.md`）。

**稳定性与安全**
- 两路只读审计共修 54 条缺陷：nginx reload 失败不再静默（写盘→改属主→reload→**请求级复核**）、
  phpMyAdmin 入口统一带访问守卫、Colima 数据目录挂载不再丢、下载/健康检查失败不再谎报成功。
- 仓库里残留的**旧面板口令**已清理；新增门禁「真实凭据不许进仓库」，并已轮换面板口令。

---

v0.9.7 · 镜像优先 + 探测不通自动回落 + nginx 启动自愈（含反代功能页）

**镜像优先（用户要求）**：面板设置里的镜像基址（默认 `https://mirror.zizdog.com:8888`）
是第一优先，凡是镜像上有的资源都从它取：
- 应用市场安装包 → `<镜像>/apps/<应用>/<版本>/<文件>`；
- 命令行开发者工具 CLT → `<镜像>/clt/…`（不通再走苹果 softwareupdate → 弹窗）；
- **Qwen3 TTS 模型（约 2.9GB）** → `<镜像>/hf/…`（NAS 按需缓存 → hf-mirror.com），
  不通回落到直连 `hf-mirror.com`。
判定按**具体资源**做（HEAD 那个文件），所以"镜像活着但缺这个包"也会回落；
每次下载都在任务步骤里写明用了哪个源、为什么没用镜像。

**nginx 启动自愈**（两条真机坑）：
- `brew reinstall nginx` 会还原 nginx.conf，**丢掉面板的 `include vhosts/*.conf`** ——
  站点/反代配置"文件在、服务在、就是不生效"。现在每次启动自愈补 include（幂等 + 先备份）；
- nginx 需要的运行时目录（`var/log/nginx`、`var/run`、`var/run/nginx/*_temp`）
  改为**从 nginx.conf 解析出全部路径**并补齐，只对新建目录改属主。

**独立「反向代理」功能页**（侧边栏 🔀）：监听端口 + 域名/路径 → 目标地址，
不需要站点目录；带实时状态（端口是否在听、目标是否可达）、测试连通、
WebSocket 开关、Host 策略、同端口冲突拦截、停用即移除配置。真机已验证：
`http://192.168.1.4:18090/zizpanel/manifest.json` → 200。

**真机演练：卸载面板 → 网络重装 → 已装软件与配置全部保留**（2026-09-16，mini）
- `sudo /opt/zizpanel/uninstall.sh`（默认不 purge）→ 面板程序与 plist 移除、**数据保留**；
- 卸载后立刻核对：音色接收端 8899 仍 200、Orbien 8020 仍 401（已启动待鉴权）、
  控制口 9527 仍在听、6 个已装服务的 plist 与应用目录全部在、brew 包未动；
- `curl -fsSL https://zizdog.com/zizpanel/install.sh | sudo bash` 重装 →
  面板 0.9.7 起来，**配置与后缀没变**（`6zfxgccj`）、升级源与镜像基址保留、
  用原口令直接登录成功；
- 市场里 7 个应用的状态**被如实识别**（6 个已纳管 + nginx 显示"已安装"）、
  反代规则还在。也就是说：**重装后不需要重新装任何东西**。

---

v0.9.6 · 新增独立「反向代理」功能页 + 安装/卸载/重装全过程可见 + 仪表盘图标与市场描述精简

**1. 新功能：独立的「反向代理」页（侧边栏 🔀）**
- 与「网站管理」分工明确：网站 = 域名 + 根目录 + PHP；反代 = 监听端口 + 域名/路径 → 目标地址，
  **不需要站点目录**（多数用法就是"把这个端口转到另一台机器"）。
- 每条规则显示**实时状态**：端口是否真的在听、目标是否可达（附耗时）；
- 保存前可「🔍 测试目标」，不用先保存再猜；
- 默认代理 WebSocket（关掉会"能打开但用不了"）；Host 头默认改成目标主机（透传常导致 404，可切换）；
- 生成的配置写 `vhosts/proxy-<id>.conf`（与站点文件不撞名），停用即移除配置并 reload；
- 同端口 + 同域名/路径的冲突**保存时就拦**并说明原因（nginx 只会用先加载的那条，静默失效最难查）。

**2. 安装/卸载/重装：状态及时更新**
- 安装、卸载、建站完成后**立即静默刷新**卡片状态（不再需要手动刷新页面）；
- 明确弹出结果提示（成功/失败原因）；
- 新增「重装」入口：复用已下载产物、保留数据、只重建服务定义，不必先卸载。

**3. 仪表盘 CPU 卡片图标 🧠 → 💻**（用户要求）。

**4. 应用市场描述精简**：卡片只显示一句话，完整说明移到悬停提示；
新增测试 `TestCatalogDescriptionsStayShort` 防止以后又膨胀。

---

v0.9.4 · 应用包镜像：安装前先检查镜像站，镜像上没有就明确失败（不再走 GitHub）

**需求（用户明确要求）**："所有安装过程先检查 https://mirror.zizdog.com:8888 的资源能不能访问，不能再走其它。"

- 新增「应用包镜像基址」配置（设置页可改可清空，默认 `https://mirror.zizdog.com:8888`）+
  探测超时（默认 4 秒）。**留空 = 关闭镜像**（回到内置的公网/国内镜像，仅用于镜像站故障时应急）。
- 布局固定为 `<基址>/apps/<应用>/<版本>/<文件名>`，同目录放 `manifest.json`（sha256/大小）。
- 安装 Lucky / frps / frpc / Orbien 之前先 HEAD 包与清单：**缺任何一个就明确失败**并提示
  `make sync-apps`，不回退 GitHub —— 回退会让"这台机器到底能不能装"变得不可预测。
- 校验改用镜像清单里的 sha256（比原来只对 frp 校验更严：Lucky / Orbien 上游没有 checksums 文件）。
- 新增 `tools/sync-nas-apps.sh` + `make sync-apps` + `cmd/zizpanel-assets`：把 5 个应用包同步到
  NAS；应用与版本从代码注册表导出，不手抄。脚本已纳入 `make check` 门禁（bash -n / 多字节变量 / shellcheck）。
- `internal/services/mirror_test.go`：5 条测试锁住布局、镜像唯一性、缺资源时的报错文案与 sha256 校验。

**本版其它改动（同一轮）**

- **Lucky 改用 Docker 跑**（用户 2026-09-16 决定）：目录条目从原生 release 二进制改成
  compose，官方镜像 `gdy666/lucky`（实测 `docker image inspect` 报 **linux/arm64 原生**，
  容器 24MB；`docker run` 后 `LuckyWeb Start success`、16601 立即 200）。
  compose：16601 端口映射 + 配置卷 `./data:/goodluck` + `unless-stopped`。
  代价写进了条目说明：Docker 在 Colima 的 Linux 虚拟机里，反代/端口转发的目标是
  那个网络命名空间看到的东西，要连 Mac 本机服务得用宿主机地址。
- **compose 应用的"残留数据"也能清理**：与 0.9.3 修的原生类问题同源 ——
  卸载（保留数据）后卡片显示"未安装"却没有任何清理入口。
  现在 `artifacts` 会把 `<WorkDir>/compose/<app>` 报出来，卸载计划给
  `installer` + `data_paths`，前端据此显示「删除残留数据」。
- **镜像基址设置接口打通**：0.9.3 只落地了配置与安装前检查，设置页读不到也写不进
  （`GET/POST /api/v1/settings` 没这个字段），当时只能改服务器上的 `config.json`。

**尚未接入**：pip / HF / brew 三个来源仍走内置的国内镜像 —— 等 NAS 上把 `/pypi`、`/hf`、
`/brew` 反代配好再接（接早了会让 Qwen / IOPaint 直接装不上）。
**镜像站现状**：NAS 上已有静态站（`:8090`，由 `mirror.zizdog.com:8888` 对外）与
按需缓存代理（`:8091`，静态没有的会去上游取回来并落盘），nginx 已接回退。

v0.9.3 · 应用市场：卸载（保留数据）之后必须能重装

- 根因：后端把"磁盘上有安装产物"也算成已安装，于是卸载（保留数据）之后卡片永远停在
  "已安装·服务未注册"，既没有「安装」入口也没法重装（用户原话："卸载完成后连安装的
  入口都没有，用户怎么重装"）。
- 修法：`installed` 只认服务记录 / launchd plist / brew formula；产物单独用 `artifacts` 报出来，
  卡片对"残留数据"给「安装」+「删除残留数据」；卸载任务的成败显式提示并立即刷新列表。
- 回归测试：`TestMarketResidualDataOffersReinstall`（新增）、
  `TestMarketDetectsOrphanInstall`（旧断言正是这条 bug 的规则化，已改锁新契约）。

---


---

更早的更新说明（v0.8.7 及以前）已从本文档移除，历史见 git 提交记录。
