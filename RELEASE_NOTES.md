v1.1.6 · 一键 LNMP 先选版本 + 检查更新收进「面板设置」+ Python 版本可选

**一键 LNMP 现在先让你选版本**：点按钮不再立刻开装，而是弹窗让你选 nginx / PHP / MySQL
各自的版本（会标出推荐项与"已经装好、本次会跳过"的项），确认后才开始。候选来自面板的应用
目录 —— 目录里有新版本，弹窗里就会自动多一个选项。PostgreSQL 不在其中：它的数据目录与账号
体系是独立的，一键 LNMP 的收尾（默认站点、MySQL 初始化与 root 口令）对它无效，请到
「应用 → 网站环境」单独安装。

**「检查更新」回到「面板设置」里**（第 4 个 Tab，排在访问与安全 / 文件与终端 / 账号与两步验证
之后）。之前侧栏有一个入口、设置里又有一个，重复且让人困惑。同时删掉了卡片里与横幅重复的
「立即检测 + 一键更新」两颗按钮，只保留：横幅里的「一键更新到 vX」（有新版本时出现）与标题栏
的「检查更新」。侧栏「面板设置」上仍会有"新"的小红点，刷新后也在。

**升级过程看得见了**：升级时显示阶段条（检查更新 → 下载安装包 → 校验并暂存 → 替换程序 →
重启并验证）、当前阶段、**实时下载进度与百分比**、后端记录的每一步及时间，以及已用时长。
面板重启期间页面会自动重连并刷新，不需要你盯着"升级中"猜。

**Python 版本可按需选择**：应用市场上架 Python **3.10 / 3.11 / 3.12 / 3.13** 四个版本，各自
独立、可并存，装完是命令行解释器（面板自己的两个 Python 服务默认仍用 3.11，
原因是 IOPaint 依赖的 Pillow 9.5.0 没有 3.12/3.13 的 macOS 安装包）。**Intel Mac 请用
3.10 或 3.11** —— 上游从 3.12 起不再提供 macOS Intel 安装包。装完任务日志里会写出这次
**实际装上的补丁版本**（例如 `python@3.11 3.11.16`），不写"预期值"。

**镜像更可靠**：修掉了"只看到 HTTP 200 就认为镜像可用"的判据 —— 中科大镜像在只有 IPv4 的
网络下会对软件包返回"200 但 0 字节"，以前会被当成好源。现在判据是"200 且真的有内容"，并会按
Homebrew 的真实文件名规则（含 `%40` 与重打包后缀）去探测；官方源永远排在国内镜像之后兜底；
离线模式（仅走 NAS）在不满足条件时明确报错，绝不偷偷出网。镜像站上也预置了 4 个 Python 版本
及其依赖，装这些包会命中本地镜像（快很多，也不再受上游当时是否同步影响）。

**升级方式**：面板「检查更新」→ 一键更新。
