v1.3.4 · 启动自愈全局 client_max_body_size（brew 重装/升级 nginx 还原 nginx.conf 后自动补回）+ phpMyAdmin 反代边收边转发、超时给可操作错误页 + phpMyAdmin 导入不再被面板那 30 秒读超时掐断 + 面板自己的 SQL 导入改为任务中心（真实进度/可中断）+ 修「nginx 起不来：unknown "zp_scheme" variable」（默认站点/phpMyAdmin 参数块引用了变量却没定义） + 默认站点恢复纯静态（只建 index.html，不再放 index.php）+ 修 mini 上「导入 SQL 报 500」的真根因（默认站点复核认不出 PHP 版首页 → 上限任务半途失败 → php-fpm 从未重启） + 解压/打包有真实进度（走任务中心，关掉窗口也不丢）+ 状态行不再出现 null（且全就绪时不再念 LNMP 说明）+ 修 phpMyAdmin 登录不上/导入 500（反代没改写 Cookie 路径）+ 「你主动停的服务」不再算成「需要处理」 + 新增「大文件上传自检」（解决"上传数据库 500"的定位问题）+ 「上传大小 / 执行时间」弹窗修好（之前点开是空的）+ 配置文件编辑器支持查找 + 安装前明确告知并一并安装依赖 + 镜像局域网兜底（性能调整表单）+ 「编辑配置文件」不再变灰 + 「上传大小 / 执行时间」成为一键功能 + 图片压缩（libvips）上架 + 网站环境状态不再误报"未就绪" + 应用市场不再卡 15 秒 + 文件上传重做（4GiB/进度/上传文件夹）+ 装完自动建默认站点 + phpMyAdmin 413 修复

> 这个文件**只放当前这一版的更新说明**：`make release` 会把它整份写进清单，
> 用户升级时看到的就是它。历史版本说明见 git 历史（`git log RELEASE_NOTES.md`）。

**"重装 nginx 之后导入就卡死"这一类，从此不可能再发生。**
`brew reinstall/upgrade nginx` 会把 `nginx.conf` 还原成 brew 出厂版 —— 面板加进去的
`include .../vhosts/*.conf;` 与**全局 `client_max_body_size`** 会一起消失；后者消失后
phpMyAdmin 用的就是 nginx 出厂的 **1m**，4MB 的 SQL 在 nginx 层就被拒，
表现就是"点了导入一直转圈"（MySQL 里当然什么都看不到）。现在面板启动时的 nginx 环境
自愈会**一并把全局上限按配置值补回去**（幂等：一致时只读一次文件、不做改动）。

**phpMyAdmin 反代：边收边转发 + 超时给可操作的错误页。**
此前是"缓冲后再写"，慢页面在浏览器上就是白屏转圈，而且**没有上限**（能无限转）。
现在 100ms 一次 flush（正文分段出现），页面请求 2 分钟、导入 15 分钟为上限；
超时后给一张明确的错误页：**最可能是 PHP 里有卡住的请求 → 去「网站管理 → 🐘 PHP 环境 → 重启」**，
并列出 nginx / phpMyAdmin 三步排查。

**phpMyAdmin 导入：面板不再掐断它的请求。**
面板全局设了 `ReadTimeout 30s`（防悬挂连接），而它算的是"**读完整个请求体**"的绝对截止时间 ——
phpMyAdmin 的导入是一个大 body 的 POST（浏览器 → 面板 → nginx → PHP），稍慢一点就会在
**面板这一层**被掐断：页面一直转圈、既没成功也没报错（你遇到的"卡死"）。现在这条路由
单独把读截止时间推后（与文件上传用的是同一道保护）。

**面板自己的「数据库 → 导入 SQL 文件」现在是任务中心里的任务**：不再是一个长请求，
按**真实字节数**报进度（`已送入 1.2 MB / 4.0 MB（30%）`），关掉窗口也能找回、可以中断。
这条路完全不经过 phpMyAdmin 和 PHP（面板直接把文件喂给 `mysql` 客户端），
所以不受会话锁 / PHP 超时 / nginx 上限影响。

**修「nginx 直接起不来：`[emerg] unknown "zp_scheme" variable`」—— 重装 nginx 也没用的那个。**
根因：面板给默认站点 / phpMyAdmin 入口生成的 FastCGI 参数块里引用了 `$zp_scheme`、
`$zp_https`（REQUEST_SCHEME / HTTPS 两条），但**没有**那几条 `set`（定义）。
站点 vhost 里恰好有（走的是另一条生成路径），所以"本机能跑、mini 起不来"：
你这台机器上没有任何站点 vhost 定义过它 → nginx 加载配置时直接 emerg 拒绝启动。
而配置是面板写的，所以 `brew reinstall nginx` 当然无效。
现在那个块**自己带上定义**，并且加了一条门禁：**凡引用就必须定义**（遍历所有生成器输出，
逐块检查）。升级到本版后在面板里点一次「网站管理 → ⋯ 更多 → ♻️ 重建全部配置」
（或「整理默认站点」）把配置重写一遍即可。

**默认站点回到"纯静态、只要一个 index.html"（按你的要求）。**
上一版之前，phpMyAdmin 安装器会顺手往默认站点里放一个 `index.php`，而它在 nginx 的
`index` 指令里排在 `index.html` 前面 —— 结果：① 首页永远由 PHP 渲染，而
"默认站点真的生效了吗"的复核是靠 `index.html` 里的标记，于是**永远失败**；
② 那一步失败还会中止整个上传上限任务（你 mini 上"导入 4MB SQL 就 500"的根因，
见下面 v1.3.0 那条）。现在：**面板不再创建默认站点的 index.php**，
生成的 vhost 里 `index.html` 排在前面，phpMyAdmin 用的仍然只是它自己的
`/phpmyadmin/` location（那里照旧有 PHP，与首页无关）。有单测锁死这两条。

**修 mini 上"导入数据库文件一点就 500"的真根因。**
链条是这样的：默认站点的**请求级复核**是"取 `http://127.0.0.1/` 的正文，看有没有面板标记"；
而面板同时会建 `index.php`（你要求"默认站点要能跑 PHP"），它排在 `index.html` 前面 ——
装了 phpMyAdmin 后首页永远由 `index.php` 渲染，而那一版 **PHP 页面里没有那段标记**，
于是复核**必然失败**（你看到的"共探测 31 次，最后一次本机首页返回 200"）；
更糟的是那一步当时失败即**中止整个上限任务**，于是 `99-zizpanel-limits.ini` 虽然写好了，
**php-fpm 从来没被重启** → PHP 上传上限仍是出厂 2M/8M → 导入大 SQL 直接 PHP 致命错误 → 500。
三处都修了：① PHP 版默认站点正文里补上标记（有单测锁死，两份占位页都必须含它）；
② 任务顺序改成"**先重启 php-fpm**，再做默认站点"，且默认站点失败只警告、**不再让整件事半途而废**；
③ 复核失败时如果根因是"nginx 没 include 面板的 vhost 目录"，面板会直接说出这一句
（用 `nginx -T` 的真实加载配置判断，不再只报"新配置未生效"）。

另外修掉一个长期刷屏的 404：面板探测 PHP 版本时用了 `Host: localhost`，会被 Homebrew
自带的默认站点（`server_name localhost`）按名字抢走，导致 `__zp_ver.php` 404；
现在用 `Host: 127.0.0.1` 落到 default_server（面板的默认站点）上。

**解压 / 打包不再是"一点反应都没有"。**
900MB 的压缩包解压过去是**同步**跑的：请求一直挂着、页面没有任何输出、你刷新一下
还会把解压**杀掉**（任务挂在请求上）。现在两条都走**任务中心**：
立刻返回、逐条显示"已解压 128/940：www/app/index.php"，关掉窗口也能在任务中心里
看进度与结果；参数错误（没有选中项、格式不支持、目标目录越界、压缩包里有 `../`
穿越路径）会在**开任务之前**当场用中文说清，而不是丢进后台再失败。

**「上传大小 / 执行时间」现在真的落在你打开的那份配置文件里。**
之前它只改**面板生成的**站点 vhost 与 PHP 的 conf.d 片段，`nginx.conf` 里那一行保持原样 ——
你打开 nginx.conf（或 php.ini）看，当然"什么都没变"，于是看起来像"保存没用"。现在：
- 保存会把**全局值写进 `nginx.conf` 的 http 块**（备份 → 改写 → `nginx -t` → 失败自动回滚）；
- 「当前生效值」表把**每一份真实文件**都列出来（nginx.conf 全局、每个站点 vhost、
  面板的 PHP 片段），每行都能点「打开」直接看磁盘内容；
- 同时把 brew 的 `php.ini` 值也摆出来对照，并写明**面板刻意不改它**（升级会覆盖、
  手改会丢）——PHP 真正读取的是面板写的 `98/99-zizpanel-limits.ini`，右边的回读值就是它；
- 不是面板生成的 vhost 会标成「面板不改这个文件」（它里面的值会覆盖全局值），
  并且**不计入**"未生效"，不会再出现"点不绿"的假故障。

**状态行末尾那个 `null` 没了；环境都正常时也不再念"一键 LNMP 会先确保运行依赖…"。**
那是条件渲染写成了 `h(...) : null` 而外层用的是原生 `append`（它会把 `null` 渲染成
文本 "null"），已改成 `appendAll`/if 分支；那段 LNMP 说明现在只在**环境不完整或状态未知**
时才出现（全都在跑的时候念它，像是面板在劝你重装）。

**phpMyAdmin 登录不上、"一点导入就 500" —— 修好了。**
报错是 `Failed to set session cookie. Maybe you are using HTTP instead of HTTPS`。
根因不在 HTTPS，而在**反代没有改写 Cookie 路径**：phpMyAdmin 按自己的路径写
`path=/phpmyadmin/`，而浏览器访问的是面板入口 `/<安全后缀>/phpmyadmin/` —— 路径对不上，
浏览器**不会**把 Cookie 带回来，于是每个请求都是新会话：登录页提交后没有会话、
导入 POST 也没有会话与令牌，于是 500。现在面板反代会把 Cookie 的 path（以及重定向地址）
补上面板前缀，会话能正常保持。这条也有单测盯着（用假上游打回真实响应头再断言）。

**你自己停掉的服务，不再被算成"需要处理"。**
以前只要健康检查答不上来就进「需要处理」，于是你手动停了 Qwen3 TTS / 音色接收端之后，
它们被标成故障、还提示"检查未通过、改检查地址"。现在面板会**记下你的启停意图**：
你点「停止」= 你不想让它跑 —— 这类服务不做健康检查、不计入「需要处理」，
卡片上写明"已停止（你手动停的）"并给一颗「▶ 启动」。升级时会按面板自己的操作记录
（审计日志里最后一次成功的启停动作）**回填**一次，所以你之前手动停掉的那两个
也会立刻归位。**崩了的服务照旧会被标出来**——这两件事必须分得清。

**新增「🩺 大文件上传自检」**（在「⚡ 上传大小 / 执行时间」弹窗里）。
上传数据库报 **500** 与报 **413** 是两件完全不同的事：413 是上限太小（nginx 层直接拒收），
**500 是请求已经被接受、随后某一步真的失败了** —— 最常见的是 nginx 把请求体缓冲到磁盘时
**磁盘满了**，其次是临时目录权限不对、PHP 进程内存不足。自检会直接列出：
磁盘可用/总量（并对比你设的上传上限）、nginx 请求体临时目录路径与 worker 用户可写性、
以及 **nginx error_log 里最近与上传相关的原始错误行**（例如
`write() failed (28: No space left on device)`）。这样不用猜、也不用登录终端。

**修：「⚡ 上传大小 / 执行时间」弹窗点开是空的。**
上一版把这个弹窗的主体建在了另一个函数的作用域里，打开时脚本报错、正文一片空白
（你截图看到的正是它）。现在正文正常：nginx 请求体上限 + PHP 上传/单个/请求体/内存/执行时间，
保存后自动重载 nginx、重启 php-fpm 并回读生效值。**这类"点开空白"的问题已加进端到端门禁**
（每次发布前真的用浏览器点开一次，正文没有关键项就失败）。

**配置文件编辑器加了查找。** 编辑 nginx.conf / php.ini / my.cnf 时，上方有查找框：
回车（或「↓ 下一个」/「↑ 上一个」）跳到下一处，命中会被**选中并滚到可见**，
右侧显示"第 n/m 处"。

**安装应用时会**事先**告诉你会一并装哪些依赖。** 例如 TTS 会在安装检查里写明
"依赖：ffmpeg（TTS 编码 mp3 必需）、python@3.11（mlx-audio 只在 3.11 有预编译 wheel）"，
并在开始安装前再确认一次；任务日志里也会逐条说明。目录里声明的依赖现在**全部**会出现
（此前只显示 ffmpeg，python@3.11 虽然会被安装却没有告诉你）。

**镜像站多了"局域网入口"兜底。** 公网镜像入口坏掉/不在同一网络时（实测公网入口
TLS 通、返回空响应），面板会自动改用同一台 NAS 的局域网入口，再去下应用包 ——
不会再出现"NAS 上明明有包却装不上"。可在「面板设置 → 应用包镜像」里改或清空。

**新增宝塔式的「⚙️ Nginx 管理」**（网站管理工具条，或 nginx 应用的管理面板）。
四个页签：**服务**（版本/运行状态/站点数，启动·重载·停止·校验）、**配置修改**（打开 nginx.conf 原文）、
**性能调整**、**错误日志**。性能调整就是那张常用表单：worker_processes、worker_connections、
keepalive_timeout、gzip（开/关）、gzip_min_length、gzip_comp_level、**client_max_body_size（MB，
最大上传文件）**、server_names_hash_bucket_size、client_header_buffer_size、client_body_buffer_size。
保存时会先**备份** nginx.conf（`.zizpanel.bak`），再写盘 → `nginx -t` → 重载；
**校验不通过会自动回滚**（绝不会留下一个起不来的 nginx）；随后用 `nginx -T`
**回读真实生效值**告诉你哪一项真的生效了，被别处配置覆盖时也会明确指出来。

**「📝 编辑配置文件」不会再是灰色的了。**
之前它只在"面板里有这条服务的记录"时才可点 —— 而 nginx / PHP 完全可以
"装着、在跑、面板里没有记录"，于是最常用的那颗按钮在最需要它的机器上是**灰的**，
想改 `client_max_body_size` / `upload_max_filesize` 只能自己去改文件。
现在配置路径一律由面板从应用目录算出来（与有没有记录无关），按钮始终可点。

**"一次能传多大 / 脚本能跑多久"做成了功能，不用再改配置文件。**
「网站管理」工具条多了一颗 **⚡ 上传大小 / 执行时间**（nginx 与 PHP 的管理面板里也有）：
nginx 请求体上限、PHP 上传/单个/post_max_size/memory 上限、最大执行时间一起改，
保存后自动重载 nginx、重启对应的 php-fpm，并**回读生效值**告诉你哪一项真的生效了
（不是只说一句"已保存"）。默认值 512m/512M 就是按"能导入大 SQL"选的。

**新增应用：🖼️ 图片压缩（libvips）。**
原生 arm64 包（`brew install vips`），装完在「文件管理」工具条点「🖼️ 图片压缩」：
可选质量、最长边、输出格式（保持原格式 / WebP / AVIF / JPEG / PNG）、是否保留元数据、
是否包含子目录；默认**另存为 `xxx.min.<ext>`、不动原文件**（覆盖原文件要先确认）。
压缩在任务中心跑，每个文件一行"省了多少"，**压完反而更大的文件自动保留原样**
（绝不把你的文件弄大）。不需要 Docker、Node 或 PHP。

**"网站环境：未就绪（未检测到运行中的：nginx）"这个误报修好了。**
你的网站明明跑得好好的，面板却说没就绪 —— 因为那一行过去只看**面板自己的服务记录**，
而 nginx 完全可以"装着、在跑、面板里没有它的记录"（自己装的，或换机后记录丢了）。
现在"在不在跑"一律看**运行体证据**（nginx 主进程、PHP-FPM 端点 socket、
MySQL 端口能不能连），面板记录只用来回答"归不归面板管"：
- 在跑 → 显示"已就绪"，并在下面注明"nginx 正在运行，但面板里没有它的服务记录
  （不影响网站运行）；需要面板管它就到「应用」里对同名条目点「接入」"；
- 真的没进程 → 才算"未就绪"；
- 探测本身失败 → 显示"状态未知"，**绝不**因此说你环境没就绪。

**应用市场不再是"正在读取应用目录…"卡住不动。**
真机实测问题：打开「应用」要等 **15 秒**（冷）/0.25 秒（热）。根因不是缓存，
而是列表对**每一条**应用都跑了一遍"完整卸载计划"，其中包含一次真实的
`brew uses --installed`（Homebrew 自己启动就要 0.4 秒），36 条串起来就是十几秒。
现在列表只算便宜的部分；**依赖检查推迟到你真的点「卸载」时**（按钮上会显示
"检查依赖…"，约半秒），那时才告诉你"谁在用它、能不能强制卸载"。
修复后实测 **0.33 秒**。附带一条不变量：以后任何"昂贵探测"（brew / docker / 网络）
都不许再放到列表或首屏渲染路径上（有测试遍历全部前端代码锁死）。

**文件管理上传重做**（用户报"点上传没反应、没成功也没提示"）。三个独立原因都修了：
- 前端上传代码第一句就抛异常，抛在提示之前 → 任何大小的文件都是**彻底无声失败**；
- 全局 30 秒读超时会把慢上传直接掐断（978MB 必然失败）→ 大请求路由改为按需延长，
  实测 **48.9 秒传完 19MB 正常成功**；
- 上限从 512MB 提到 **4 GiB**，超限在**上传前**就拦下（一个字节都不发）。
现在上传有**真实进度**（百分比 / 速度 / 预计剩余 / 已用时间 / 可取消），
支持 **⬆ 上传文件夹**（保留目录结构）与**拖拽文件夹**，
失败一律有明确原因（`HTTP 413` + 服务端原文、"连接中断"等），没有静默路径。

**phpMyAdmin 导入不再 413。** 出厂 nginx 的请求体上限只有 1m，几十 MB 的 SQL
在 nginx 层就被拒（根本到不了 PHP）。现在面板生成的 vhost 默认 `client_max_body_size 512m`，
并写 PHP 自己的 `conf.d/99-zizpanel-limits.ini`（上传/内存 512M、执行时间 300 秒），
可在「**面板设置 → 上传与执行限制**」里改（保存后回读生效值，读不到就如实说"未复核"）。
「网站管理」还多了「**⚙️ 配置文件**」入口：nginx.conf、各站点 vhost、
每个 PHP 版本的 php.ini / php-fpm 配置、MySQL 的 my.cnf 都能在面板里直接改。

**装完面板就有一个默认站点（纯静态，不需要 PHP 与 MySQL）。**
面板启动时会自动建好 80 端口上的默认站点（`www/localhost` + 面板自己的 vhost），
并**请求级复核**首页真的能打开。三条边界写清楚：
- 机器上还没有 Nginx → 如实显示"默认站点待创建（缺 nginx）"，
  「🏠 默认站点」里可以**只安装 Nginx**（不装 PHP/MySQL），装完自动接着建站；
- 80 端口上已经有一份**你自己写的**默认站点 → 面板**绝不自动覆盖**，
  只提示；你要换的话在弹窗里明确点「覆盖为面板默认站点」，**原文件会先备份**成 `.zizpanel.bak`；
- 已经有面板默认站点的机器（升级上来的）→ 一个字节都不动，也不会重载 nginx。

**顺带修的**：文件管理打开时的默认目录以前会落到 Homebrew 的配置目录
（`/opt/homebrew/etc`，代码注释写的是"网站根目录"）—— 现在是网站根目录。
升级包上传（256MB）与音色样本上传（20MB）同样受 30 秒读超时影响，一并修好。

**升级方式**：面板「面板设置 → 检查更新」→ 一键更新。
