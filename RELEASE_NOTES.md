v0.6.5

新增「系统设置」页（取代原来的「系统监控」）
- 仪表盘与系统监控内容高度重复，监控页已改造为**系统设置**：直接读写 macOS 的服务器化配置。
- 原监控页独有的「系统负载」「Swap 使用」已并入仪表盘「系统信息」，没有丢信息。
- 主机状态：机型、面板是否 root、SSH 是否在听、自动登录、Tailscale。
- 一键设为服务器模式：电源策略 + 阻断系统更新 + 静默诊断 + 关 Spotlight 索引 + 开 SSH。
- 电源与睡眠：sleep/disksleep=0、womp=1、powernap=0、autorestart=1、displaysleep=10；
  用 `pmset -g cap` 探测本机是否支持某个键，**不支持的机型直接标明**（不提供点了会静默无效的按钮）。
- 系统更新阻断：清掉安装类偏好 + 把 8 个更新域名写进 hosts 指向 0.0.0.0；
  三个按钮：彻底阻止 / 验证阻断（真的去问一次系统）/ 恢复更新。
- 崩溃报告弹窗与自动上报、Spotlight 索引开关、开启 SSH、关闭自动登录。
- 每个动作都走任务中心：能看到每条命令与输出，执行完重新读真实值复核（不看退出码）。

应用市场新增 4 个应用（选品规则：能原生就原生，不能原生再 Docker，且镜像必须自带 linux/arm64）
- IT-Tools（开发者工具箱）
- File Browser（网页文件管理）
- MetaTube Server（媒体元数据服务）
- Squoosh（图片压缩）
- 新增测试锁死「compose 里不许写 platform」——不许 Rosetta 转译。

修复两个真机上很难查的问题
- **仪表盘每重渲染一次漏一条 SSE**：`renderApp()` 用形参个数判断页面要不要注册清理函数，
  而 `DashboardView(content, ctx = {})` 的 `Function.length` 是 1，
  于是清理分支从来没走到 —— 攒够 6 条就把浏览器的同主机连接数占满，
  表现为「面板所有接口都不返回」而服务端完全正常（curl 200）。
- **日志流被服务端拒绝时界面谎称「正在重连」**：EventSource 对非 200 响应不会重连，
  现在会区分「断了会自动重连」与「被拒绝、永久放弃」，并显示真实原因。

测试
- `make uitest` 在**全新实例**上也会失败的两个问题已修：审计页筛选写死 `login`
  （干净实例只有「初始化」没有「登录」）；服务日志步骤没有断言失败时的真实文案。

真机验证时抓到的两个错误状态显示（本机 + mini 都升级）
- **电源能力探测全部误判为"本机不支持"**：`pmset -g cap` 是多行文本
  （每行前有一个空格），旧实现拿整段做子串匹配，除首尾两个键外全都匹配不上 ——
  界面会把 sleep / autorestart 等每一项都标成"本机不支持"，用户因此改不了电源设置。
  现在按行解析，并用真机原始输出做测试样例。
- **SSH 端口探测永远返回"未监听"**：`lsof` 的四个选项被拼成一个字符串当成单个参数传递，
  lsof 直接报错退出、没有输出。真机上的表现是：用户正连着 SSH，
  页面却说"22 端口没人听"。现在参数独立传递，并有单测锁死参数形状。

升级可靠性（mini 上真实踩到的问题）
- mini 升级时 launchd 里的面板 job 一度僵住：新版与回滚后的旧版都起不来，
  `launchctl` 只报 `last exit code = 78: EX_CONFIG`，日志空白；而同一个二进制
  手工执行完全正常。现场用 bootout + bootstrap 重装 job 即恢复。
  现在看门狗在等待期间会主动重装一次 job（只允许在健康检查已连续失败时，
  且必须紧跟 bootstrap 装回去，有测试锁死）。

TtsVoice 只保留「自定义音色」（克隆）—— 服务端单模型
- 网站侧插件已只支持克隆，预置音色（CustomVoice）整体下线，服务端只需要
  mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit 一个模型。
- 面板的模型清单相应减到一个：不再显示「CustomVoice（预置音色）」这个选项
  （它的权重已从机器上删除，留着只会渲染出一个点了就失败的假按钮）。
- 「驻留模型」弹窗的按钮语义从「切换」改为「加载到内存 / 释放内存」；
  仍会在服务重启后自动补载（守温，每 2 分钟一次）。
- 顺手把「驻留但已不在清单里」的旧模型释放掉（CustomVoice / 0.6B 会因此让出内存）。
- 已删除的权重：1.7B-CustomVoice-8bit（mini，2.9G）、1.7B-CustomVoice-4bit、
  0.6B-CustomVoice-8bit、0.6B-Base-8bit（本机与 mini 早已清掉）。有测试锁死"不许加回来"。
- receiver 接口未改（v1.4.0 不变）；wav + sampling + 克隆与老请求回归四条验收均通过。

应用市场：有界面的应用一键「打开」+ 子路径代理
- 每个有界面的应用都能挂到 `/<应用名>/` 下：`http://<主机>/iopaint/`（经 nginx 的 80 端口）、
  `https://<面板地址>/iopaint/`（面板自己反代）、`https://panel.zizdog.com:8888/iopaint/`
  （经隧道）三种入口都通。
- 面板进程内实现反代（internal/appproxy），一份改写规则同时服务所有入口：
  自动把前端产物里的绝对路径（`/assets/`、`/api/v1`、`/socket.io`）与
  `Location` 重定向拉进子路径；摘掉 Accept-Encoding 保证上游返回未压缩正文；
  放行 WebSocket 升级。
- 市场卡片新增「打开」按钮：先探测（不是只看 200 —— 还会取页面引用的 js/css，
  代理没改写对时页面照样 200、资源全 404 就是白屏），探测通过给子路径入口，
  不通过就把「直连端口」作为首选并说明原因（n8n / Gitea / MinIO / Stirling
  需要应用侧自己设 base path）。
- 顶部新增「生成 nginx 入口」：把子路径写进 nginx 的默认 vhost（幂等标记块 +
  nginx -t 失败自动回滚）。
- phpMyAdmin 不再显示「已安装·服务未注册」——它本来就没有守护进程
  （nginx alias + php-fpm），现在显示「已安装·网页入口」并直接给打开入口。
- 设置里可关闭「应用界面代理」（关掉后 /<slug>/ 与入口一起消失）。

应用「打开」的入口选择更诚实（0.5.3 的收尾）
- 子路径能不能用由**真机探测 + 人工实测**共同决定：探测除了看状态码，
  还会取页面引用的 js/css（只看 200 会漏掉"页面 200 但白屏"）；
  对"资源全 200、前端路由却不认前缀"的应用（Uptime Kuma）如实标记为
  子路径不可用，界面把「直连端口」作为首选入口并说明原因。
- n8n / Gitea / MinIO / Stirling 同样标注：它们需要在应用侧自己设 base path。
- 点「生成 nginx 入口」后会等 nginx 完成切换再重新探测（reload 是异步的，
  立刻探测会拿到旧结论、把可用误报成不可用）。

应用市场：已安装的应用终于有卸载入口了
- 之前面板自研安装器装的应用（IOPaint / Qwen3 TTS / 音色接收端 / phpMyAdmin）
  注册时是"纳管"语义，通用卸载会拒绝卸载它们 —— 用户装了却找不到任何卸载办法。
- 现在按来源分三类：
  · 面板托管服务（compose 应用等）→ 卸载（compose 只删容器与网络，具名卷保留）
  · 面板安装器装的 → 卸载（卸服务 + 删 plist + 删记录；
    虚拟环境/模型/音色样本/历史任务默认保留，勾选「同时删除数据」才删）
  · 纳管的第三方服务（nginx / php / mysql）→ 只给「取消纳管」，
    面板绝不卸载你自己装的软件
- 卸载前的确认框会逐条列出"会执行什么、会删哪些路径、会保留什么"。
- phpMyAdmin 的卸载会先撤掉它写进 nginx 的那段 location 再 brew uninstall，
  避免留下指向不存在目录的入口。
- Docker 运行时在还有 compose 应用登记时拒绝卸载（界面与后端都拦）。

卸载的确认信息更准确（0.5.5 的收尾）
- Qwen 的待删路径改成逐个列出**清单里的模型目录**。之前写的是整个
  ~/.cache/huggingface/hub，而实现只删那几个目录 —— 确认框上写"要删整个缓存"
  会让用户以为别的项目的模型也没了。
- 原生 brew 服务的卸载说明写清楚「面板只删服务定义，Homebrew 包不会被卸载」，
  不再把 compose 的说法套在它身上。

「系统设置」两个真机验证时抓到的问题
- 「验证更新阻断」以前在任何机器上都失败：它调的是 /usr/bin/softwareupdate，
  而 macOS 上这个命令在 /usr/sbin（两台 Mac 实测）。更糟的是报错信息把
  "命令没跑起来"说成了"阻断可能没生效"，会把人带偏。现在按候选路径解析，
  找不到时如实说"找不到 softwareupdate 命令"。
- 「恢复系统更新」以前一律把 5 个偏好写成 true，会改掉用户原本的选择。
  现在阻断时把改动前的原值记进 hosts 阻断块的一行注释，回滚按原值恢复；
  键原本不存在则记 unset，不凭空写值。（/etc/hosts 的还原仍是逐字节一致。）
- 本轮在本机完整走了一遍 阻断 → 验证 → 恢复：hosts 与操作前逐字节相同。

「验证更新阻断」的判定修对了（0.5.7 的收尾）
- 上一版修好了命令路径，但在 mini 上仍报"仍能查到更新（阻断可能没生效）"。
  真因：hosts 阻断生效时 softwareupdate --list 打印「错误的URL」**却是退出码 0**，
  而判定把"看到错误文本"绑在 err != nil 上 —— 阻断有效被判成失效。
- 现在的判定（纯函数，有单测）：①输出里有更新目录不可达的明确特征 → 已阻断（不看退出码）；
  ②no new software → 也没有可用更新；③命令失败但输出不像上面两种 →
  如实说"无法据此判断"（这不是「阻断失效」）；④其余（真的列出更新）→ 没阻断。
- 两台机器的实测结果都正确：mini=已阻断，本机=确实还有 6 项更新待装（未阻断）。

Docker 页新增「加速源」：给守护进程配 registry-mirrors（换源）
- 起因：这台机器直连 registry-1.docker.io 会超时（实测拉 alpine 卡 30 秒后
  报 dial tcp i/o timeout），不配加速源基本没法拉镜像。
- 新增第 7 个分区「加速源」：
  · 先把「Docker Hub 官方源现在通不通」摆在最前面（用户抱怨的"慢"就是它）
  · 内置 13 个常用加速源，**每条都带实测结论**（哪些站已经死了如实标出：
    ustc / 163 / 百度 / kubesre / dockerpull 实测超时，南大 403，dockermirror TLS 失败）
  · 「检测可用性」用你自己的网络再验一遍：并发探测 /v2/，判定按真实回包
    （200 与 **401 都算可用** —— 401 是 registry 要求鉴权，是它正常工作的证据；
    403=被拒绝、302=跳转、超时=连不上），并显示延迟
  · 勾选后「保存并重启 Docker」：写入 colima.yaml 的 docker: 段（先备份、
    其它内容一字不动）→ colima restart → 复核 docker info 真的读到了 →
    再实测拉一个小镜像并把耗时写进日志；走任务中心（重启约 30-60 秒）
- 只对能验证的运行时动手：Colima 自动配置；OrbStack / Docker Desktop 如实告知
  面板不改写它的配置（避免写一个没验证过的文件）。

加速源排序（0.5.9 的收尾）
- docker 只用**第一个能应答**的镜像源（不会因为某个源慢就自动换下一个），
  而按字母序写进去时排第一的未必最快。真机实测：docker.1ms.run 排第一时
  拉 alpine/busybox/redis 要 30s/18s/22s。
- 现在保存前会先实测一遍，按（可用优先、延迟升序）排序再写入，并把实测结果
  打进任务日志，用户能看到为什么是这个顺序。

本机 Web 入口收口 + 仿宝塔安全后缀 + 一键建站 + 若干修复

安全（用户要求）
- 默认站点不再暴露 `/_panel/` 与 `/phpmyadmin/`：面板从 80 端口下线，
  phpMyAdmin 的 nginx location 收紧到**只允许 127.0.0.1**。
- 面板新增**安全后缀**（仿宝塔「安全入口」）：安装时随机生成，
  界面与接口只在 `https://<主机>:8443/<后缀>/` 提供，直接访问根路径得到 404。
  可在「面板设置」里查看/修改（也可清空，等于关掉）。
- phpMyAdmin 必须**先登录面板**才能访问（未登录返回 401 + 说明页）。
- 默认站点改为 `~/www/localhost`，只放一张占位 index.html。

应用市场
- 新增「一键建站」分类：Typecho 与 WordPress —— 自动下载源码、解压到
  `~/www/<域名>`、建库建用户、写配置文件（wp-config.php / config.inc.php）、
  建站点并**自动套用对应伪静态**，最后给出安装向导地址与数据库信息。
- File Browser：默认只挂载 Mac 的常用目录（文档/下载/音乐/影片/桌面），
  子路径改用官方 `-b /filebrowser` 修好（之前在 /filebrowser/ 下一直转圈），
  安装完成会把首次随机生成的 admin 密码直接显示出来。
- Squoosh：补上 `/c/` 资源前缀改写与 COOP/COEP 头（子路径下曾报
  "Failed to load app"）。
- Ollama：市场不再只显示"已安装"（它的 brew 服务 plist 其实不存在）；
  服务管理里点启动会自动用 `brew services start` 补注册。

其它
- 「系统设置」新增受保护目录检测（文档/下载/桌面等）：读不到时明确提示
  这是 macOS 隐私保护（TCC），并给出授予完全磁盘访问权限的路径。

安全后缀即时生效（0.6.0 的收尾）
- 0.6.0 的闸门在进程启动时读取后缀，导致"在设置里改完后缀、旧路径仍然可进"。
  现在改成**每次请求现读**：改完立即生效，不留半开半关的状态。

phpMyAdmin 入口写法修正（0.6.0/0.6.1 引入）
- 生成的 nginx location 带了尾斜杠，而 `alias` + `try_files` 组合下这会
  变成去找 `<alias>/phpmyadmin/index.php`，落成 403 directory index is forbidden。
  改回真机上一直跑通的无尾斜杠写法。面板内部的 phpMyAdmin 访问因此恢复正常。

应用界面入口不再被安全后缀拦住（0.6.0 引入）
- 安全后缀闸门最初把 /iopaint/ 这类应用入口也一起拦了（全变 404）。
  现在应用代理挂在外层，面板界面与接口在闸门之内 —— 两者互不影响。

File Browser 子路径双重前缀修复
- 它用 `-b /filebrowser` 自带前缀后，面板的 /assets/ 改写又加了一次，
  变成 /filebrowser/static/filebrowser/… → 资源全 404。
  现在这类"应用自带 base path"的条目（SelfBase）面板不做任何路径改写。
- 默认站点整理新增两处硬伤修复：写 vhost 前把 ~/www/_logs 归属改回运行用户
  （否则 nginx 打不开 access_log，reload 会失败但退出码仍是 0），
  以及 reload 后**按真实结果复核**（本机首页拿不到占位页就报错，
  而不是"写了就算成功"）。

Ollama 启动失败修复（用户反馈）
- 市场显示"已安装"、服务管理却点不动，报"找不到 homebrew.mxcl.ollama 的 plist" ——
  因为这台机器的 brew 用 `sh.brew.*` 前缀，而记录里写的是 `homebrew.mxcl.*`，
  且服务从没启动过（plist 不存在）。
- 现在点启动会先用 `brew services start <formula>` 注册服务，再按**真实 label**
  复核 plist（两种前缀、系统域与用户域都查），服务真的在跑就报成功。
