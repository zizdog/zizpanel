v1.1.11 · 模型下载真正走镜像（HF_ENDPOINT 之前被 sudo 吞掉了）+ 状态不再谎报「已停止」+ 卸载可选强制

**装 TTS 时模型"零速度"的真根因**：面板调 `hf download` 用的是
`sudo -n -u <你> <venv>/hf download …`，而 `HF_ENDPOINT`（镜像地址）是设在 **sudo 进程**上的
环境变量 —— `sudo` 默认 `env_reset` 会把它清掉，子进程收不到，于是 CLI 去连
`huggingface.co`（国内不可达），日志里"模型来源：…/hf"却看着一切正常，现象就是
`Error: Local entry not found. [Errno 60] Operation timed out`、完全没有下载速度。
现在统一改成 `sudo -n -u <你> /usr/bin/env HOME=… HF_ENDPOINT=… HF_HUB_DISABLE_XET=1 <命令>`，
环境变量真的进子进程；并且镜像端点探测升级为"**API 返回 JSON + 文件真能取到**"双条件
（此前只看 `/hf/` 首页 200，会被"首页正常、API 坏掉"的地址骗过）。

**「已停止」不再乱贴**：没有常驻进程的应用（FFmpeg、Python 各版本、phpMyAdmin）不再显示
"已停止"，改为如实的"已安装（无常驻进程）"；本机已有、但面板里没有记录的服务（如 Nginx）显示
"已安装（面板里暂无记录）"，只有真有服务记录/开机项且状态是停止时才写"已停止"。

**卸载更可控**：
- 依赖冲突先说清：别的包依赖它时（例如 llvm、rust 依赖 python@3.13），面板会先告诉你
  "谁在用它"，并给两个选择——「取消」或「**强制卸载**（`--ignore-dependencies`，会破坏这些包）」；
  不再把 brew 的原始报错整段丢给你。
- 卸载不再顺手删别人的包：命令带 `HOMEBREW_NO_AUTOREMOVE=1`（上一版卸 PHP 时
  brew 顺手自动删掉了 net-snmp、rtmpdump，就是这个行为）。
- 卸 PHP 只清理**这个版本**的配置目录；brew 那句"配置没删"的提示会翻译清楚，
  不会再列出一堆 **8.2** 的路径让你以为要删别的版本。

**升级方式**：面板「面板设置 → 检查更新」→ 一键更新（升级后直接重跑 Qwen3 TTS 部署，
模型会走镜像/局域网下载）。
v1.1.10 · 模型下载不再"选错源"（NAS 的 HuggingFace API 修好 + 面板会真验通再选）

**TTS 装不上、下载卡住的真根因**：面板会优先用自建镜像下载模型，但它的"可用性探测"只看
镜像站 `/hf/` 首页能不能打开 —— 而 `hf download` 必须先调 HuggingFace **API** 列文件。
当时镜像站的 `/hf/api/...` 是 502（缓存里同一个名字既是目录又是文件，落盘失败），
于是面板把 `HF_ENDPOINT` 指向了一个"首页正常、API 坏掉"的地址，`hf download` 连试三次
`Error: Local entry not found. [Errno 60] Operation timed out`。

现在两头都修了：
- **镜像站**：修好同名目录/文件冲突的处理（这类请求改为透传上游、不再落盘），
  `/hf/api/...` 恢复 200；模型文件本来就缓存着，命中只要 0.01 秒；
- **面板**：把探测换成"**API 与文件两条都真能取到**才算可用"，探不通就如实回落到
  `hf-mirror.com` —— 不会再出现"指向坏源还不知道"。
（顺带说明：仪表盘的网络吞吐没有坏。实测从镜像站拉 200MB，面板显示到 44.5 MB/s；
你当时看到几 KB/s，是因为下载根本没跑起来 —— 这正是上面那条的后果。）

**升级方式**：面板「面板设置 → 检查更新」→ 一键更新。
v1.1.8 · 卸载真的会卸载 + 依赖检测 + Docker 启动修好 + 网站管理工具条整理

**「卸载」现在真的卸载软件。** 以前分两种情况都不对：① 面板装的 brew 应用（如 PHP）点「卸载」
只删了服务定义，包还在磁盘上；② brew 装了、但面板没有记录的（如 nginx、Python）**连卸载按钮
都没有**。现在：目录里的应用一律给一颗「🗑 卸载」，确认框逐条列出会做什么，走任务中心执行
（停止服务 → 删除自启配置 → `brew uninstall <包>` → 删除面板记录，可选同时删数据）。

**卸载前会做依赖检测，并告诉你先处理谁。** 例如：
- 卸载 **ffmpeg**：Qwen3 TTS / IOPaint / 音色接收端 正在用它 → 必须先卸载它们（否则 TTS 合成会
  返回 200 但 0 字节）；
- 卸载某个 **PHP 版本**：有站点正在用它 → 列出域名，请先在「网站管理」把它们切到别的版本；
- 卸载 **MySQL / nginx**：列出受影响的站点；
- 卸载 **PostgreSQL**：Miniflux 在用它；
- 卸载 **Docker 运行时（Colima）**：列出面板里的 compose 项目**和正在运行的容器**，要求先停止
 （或明确确认）——不会悄悄把还在跑的东西变成"看不见但一直在跑"；
- 卸载 **Python 某版本**：指出哪些虚拟环境（Qwen3 TTS / IOPaint）在用它；
- 兜底：`brew uses --installed` 查出还有哪些包依赖它（查不到会如实写"未检查"）。

**「点了没反应」的根因修掉了。** 卸载确认框有两份实现，确认按钮的 Promise 被弹窗关闭回调
抢先置为"取消"，于是点了「确认卸载」既不请求也不提示 —— frpc 那个"点了毫无反应"就是这个。
现在确认框只有一份实现，并且有回归断言：确认后必须真的发出请求。

**Docker 起不来修好了（真机验证）。** 一键安装 Docker 时 brew 包装好了，但 `colima start` 报
`permission denied`：面板以 root 写的 `~/.colima/default/colima.yaml` 属主是 root，而 Colima 是以
你的身份运行的。现在面板每次调用 Colima 前、以及每次写配置后都会把 `~/.colima` 交还给你本人
（旧版本已经留下 root 属主文件的机器也会被自动修好）。本机实测：修好后虚拟机正常启动，
引擎 29.5.2 可用。

**PHP 8.4 看得见了。** 本机装的是不带版本后缀的 `php`（实际 8.4.7），而面板只认 `php@8.4`，
于是「PHP 环境」里只有 8.2 —— 用户以为 8.4 没装、又找不到卸载入口（`brew uninstall php@8.4`
当然报 "No such keg"）。现在版本别名会被正确解析成 8.4，并给出对应的卸载入口。

**网站管理版块整理**：「校验 nginx」「重建全部配置」收进「⋯ 更多」二级菜单；「🐘 PHP 环境」右侧
只显示 nginx 的运行状态（运行/停止/未知 —— 面板没登记时不谎报）；**LNMP 环境完整时不显示
「一键 LNMP」**，缺什么就如实列出（含 CLT / Homebrew 这类底座），点开弹窗还会再显著提示一次，
并且**必须再点一次「开始安装」**才会执行（已装的部分会跳过）。

**升级方式**：面板「面板设置 → 检查更新」→ 一键更新。
