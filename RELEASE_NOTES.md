v1.1.5 · Homebrew 安装失败会自己换源 + 基础环境与网站环境分开

**Homebrew 装不上时不再只报一句"失败"。** 以前只要当前镜像上那个 bottled 包是坏的
（镜像侧缓存过一次空文件就会一直返回空文件），`brew install` 就必然失败，面板只会说
"安装失败"，用户无从下手。现在每次 `brew install` 都会按顺序试：**当前镜像 → 清华镜像 →
官方源**，并在任务日志里逐条写明"用了哪个源、失败的真实原因"；换源前只清掉与本次安装相关的
坏包缓存（不会动你其它下载缓存）。离线模式（仅走 NAS）下只走镜像，绝不会偷偷出网。

**「基础环境」与「网站环境」彻底分开。** 首页横幅现在只装**运行依赖**（命令行开发者工具 /
Homebrew / ffmpeg），不再顺手装 nginx+PHP+MySQL；想要 LNMP 请到「应用 - 网站环境」里一键安装。
网站管理页会显示网站环境的当前状态。

**界面结构**：侧栏「系统设置 → mac 设置」；操作审计并入「日志」（日志 / 审计两个页签）；
「检查更新」移到「面板设置」之后。

**修掉的问题**：
- 面板里点「卸载」没反应也不卸载（写操作失败被前端吞掉）：现在提交超时会明确报错，
  卸载失败会给出可照做的下一步，运行时不可用时改为"只删记录"并**如实说明没有停止容器**；
- 应用市场里已装应用的「卸载」入口消失（合并页面时丢的）；
- 装第二个 PHP 版本时两个版本互抢 9000 端口，导致"我明明选了 8.4、跑的却是 8.3"：
  每个版本现在会固定到**自己专属的监听端点**；
- 一键建站漏套伪静态预设的入口目录，会把订阅数据目录暴露到网站根目录（FreshRSS）。

**版本 1.1.x 一并包含**（1.1.0 – 1.1.4 的界面与后端改动）：首页"缺少运行依赖"横幅 +
「安装基础环境」按钮、网站环境板块、日志中心、应用市场下载点与镜像回退优化。

**升级方式**：面板「检查更新」→ 一键更新（升级包小，走公网源即可）。
