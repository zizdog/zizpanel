v0.7.2 · 音色按来源隔离（receiver v1.5.0）+ 面板「各来源」

这一版修的是一个"看上去毫无线索"的真实故障：合成失败只报
`IncompleteRead(0 bytes read)`，而根因在音色样本文件本身。

音色接收端 v1.5.0（须重新部署一次接收端才生效）
- **按来源落盘**：`POST /voice` 新增 `X-TtsVoice-Source` 头（缺省 default），
  样本存到 `<样本目录>/<来源>/ref.wav`。以前所有站点共用一个 ref.wav，
  后传的覆盖先传的 —— 两个站共用一个接收端时，用户听到的是**别的站的音色**。
- **按内容校验**：先看魔数，再用 ffprobe 真解码；解不开直接 400 并说明原因。
  以前只按请求头里的文件名判断，一个 mp3 被命名成 ref.wav 就落盘，
  上游按扩展名选解码器、解不开时返回 **HTTP 200 + 0 字节** —— 就是那个
  `IncompleteRead(0 bytes read)`。改名的假 wav 不会再被放进去。
- **统一归一化**：≤12 秒 / 24kHz / 单声道 / PCM s16le WAV，原子替换。
  <1 秒直接 400（太短的样本不像本人），1–3 秒 200 但带 warning，
  超过 12 秒自动截断并标 truncated。响应带 source/format/duration/size/sha256。
  没有 ffmpeg 时不假装成功：只放行本来就是 24kHz 单声道 wav 的文件。
- **/jobs 带 source**：取样本顺序为 `显式 ref_audio` → `<目录>/<source>/ref.wav`
  → `<目录>/ref.wav`；最后那份只给 default 兜底，具名来源没有样本就明确报错，
  绝不悄悄借用别的站的音色。source 记进作业、写进日志、随 GET /jobs/{id} 返回。

面板
- 「服务管理 → TtsVoice 音色接收端 → 详情 → 🎙 各来源」：列出每个来源
  （标识 / 时长 / 大小 / sha256 前 8 位 / **最后使用时间**），可单独替换、删除，
  也可先手动上传新来源。旧版留下的根目录 ref.wav 显示为 default（旧版单文件）。
  列表与替换都走接收端的 /voice/sources 与 /voice：解析与转码只有一处实现。
- 重新部署接收端时**沿用原有监听地址**：本机是 127.0.0.1，点"重新部署"
  不会再被悄悄改成 0.0.0.0（升级不该改变暴露范围）。
- 修掉"自定义共享密钥读不回来"：以前只认面板生成的 `ttsv-` 前缀，
  自定义密钥会被当成空串，导致换密钥另生成一个（网站立刻失联）、
  各来源页无法鉴权。现在密钥就是 plist 里 --token 的值。
- 来源标识的清洗规则在 Go 与 Python 两侧逐字一致（同一组单测用例），
  避免"面板删 A、接收端理解成 B"。

测试
- receiver：新增上传校验/来源隔离/来源列表/删除/`/jobs` 带 source 的端到端用例
  （用临时的样本目录与端口，不碰真实服务），并把 `health.version` 改为
  与脚本 VERSION 比对，升级时不会再出现"测试里写死的旧版本号"。
- Go：新增来源标识清洗与"自定义密钥可读回"的单测。

不做旧版兼容（插件仍在开发期）
- `/jobs` **不留旧版回退**：不再读根目录那份 v1.4.0 遗留的 ref.wav，
  只认 `显式 ref_audio` → `<目录>/<source>/ref.wav`，没有就明确报
  `voice sample not found`。宁可失败，也不猜"该用哪份音色"。
- 给 default 上传只写 `<目录>/default/ref.wav`，不会去覆盖根目录残留文件。
- 根目录残留文件仍会在「各来源」里单独列一行（标成"v1.4.0 残留，已不再使用"），
  方便直接删掉；删 default 会把子目录与残留一起清理。
- 时长读不出来的样本（例如被改名的 mp3）在列表里显示为「⚠️ 无法解析」而不是 0.0 秒 ——
  mini 上那份 ref.wav 正是这种状态。

说明
- receiver.py 随面板二进制内嵌，但**升级面板不会自动重写已经在跑的那份脚本**。
  要让 v1.5.0 生效，请在「应用市场 → 音色样本接收端」重新部署一次
  （会复用原密钥、沿用原监听地址）。没升级时「各来源」会明确提示"版本过旧"。
